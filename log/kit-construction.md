# Journal de construction — Système de design agentique Kit

> Toutes les modifications aux fichiers de configuration, règles, et décisions du kit.
> Mis à jour automatiquement par hook `PostToolUse` à chaque modification.
> **Hors scope :** modifications de contenu (`tokens/`, `guidelines/`, `components/`).

| Horodatage       | Action  | Fichier |
| 2026-07-06 | fix(tokens)+chore(figma): audit et sync complète Figma ↔ code (1 470+ variables) | **Fichier Figma "Agentica — Agentic Design System" audité intégralement contre `tokens/*.json` via MCP Figma (skill `figma-generate-library`), à la demande explicite de l'utilisateur — aucun fichier `tokens/*.json` n'a été modifié, seul Figma a été corrigé pour s'aligner sur un code déjà correct.** **Dérives de valeur trouvées et corrigées (2)** : (1) `primitive/fontSize/*` (10 variables) stockait la valeur rem×4 au lieu du px réel (`xs=3` au lieu de `12`, `6xl=15` au lieu de `60`) — aucun Text Style n'y était lié donc pas d'impact visuel actuel, mais toute future liaison directe aurait produit un texte 4× trop petit. (2) `semantic/space/compact/*` et `comfortable/*` (10 variables) valaient `0` au lieu des valeurs calculées floor/ceil de densité (ADR-025) — les formules Tokens Studio (`floor()/ceil()`) n'avaient jamais été résolues côté Figma, ce qui aurait effondré tout padding/gap en densité compacte ou confortable. **Audit complet des 654 tokens couleur (427 primitifs + 67 sémantiques + 123 composant + 37 semantic.dark) et des 10 Text Styles / 4 Effect Styles : 0 autre dérive, concordance exacte avec le code.** **Scopes `ALL_SCOPES` corrigés sur 822 variables** (168 non-couleur + 654 couleur) → scopes explicites (`GAP`, `CORNER_RADIUS`, `FONT_SIZE`, `FONT_WEIGHT`, `LINE_HEIGHT`, `FONT_FAMILY`, `WIDTH_HEIGHT`, `TEXT_FILL`, `FRAME_FILL`/`SHAPE_FILL`, `STROKE_COLOR`, `EFFECT_COLOR` pour les primitifs semi-transparents blanc/noir), primitifs cachés (`scopes:[]`) conformément aux bonnes pratiques du design system builder Figma. Collection `figma` orpheline (0 variable, résidu sans lien avec la hiérarchie primitif/sémantique/composant) supprimée par l'utilisateur directement dans Figma. Diagramme FigJam "Hiérarchie des tokens" (créé via `figma-generate-diagram`) traduit en anglais à la demande. |
| 2026-07-06 | fix(site/layout): main class="content" manquant — pages sans sidebar (audit, changelog, get-started…) | **Régression introduite lors de l'unification layout.** La logique `!sidebar ? '' : fullWidth ? 'page-content' : 'content'` donnait `class=""` à `<main>` quand `sidebar=null`, quelle que soit la valeur de `fullWidth`. Les pages sans sidebar et sans `fullWidth` (audit.html, changelog.html, get-started.html, ai-brief.html) perdaient `class="content"` → zéro padding latéral (visible sur la page Audit WCAG). Fix : `!fullWidth ? 'content' : sidebar ? 'page-content' : ''`. Table de vérité : `fullWidth:false+sidebar:null` → `content` ✓ ; `fullWidth:false+sidebar:X` → `content` ✓ ; `fullWidth:true+sidebar:X` → `page-content` ✓ ; `fullWidth:true+sidebar:null` → `''` ✓. Audit 115 pages : 0 autre problème. |
| 2026-07-03 | refactor(site): renomme v2Sidebar → sidebarSite — zéro version dans les noms | **Convention code-style.md** : aucun préfixe de version dans les noms. `v2Sidebar` → `sidebarSite` (6 occurrences). Aligné avec `sidebarFoundations`, `sidebarComponents`, `sidebarTokens`. |
| 2026-07-03 | refactor(site/layout): unification complète layout — Pattern 1 et 2 → un seul pattern | **Fusion des deux containers de layout.** Avant: Pattern 1 (fondations/composants) = `.layout` flex avec `<aside>` hors de `<main>` ; Pattern 2 (pourquoi/ia/documentation…) = `.home-layout > main > .with-sidebar` grid avec `<aside>` DANS `<main>` (violation WCAG landmarks). Fix: les 5 builders Pattern 2 passent maintenant sidebar via `layout()` comme paramètre (plus dans le body). `.with-sidebar` CSS supprimé entièrement. `layout()` : `outerClass = fullWidth && !sidebar ? 'home-layout' : 'layout'`. `<main class>` : `page-content` si marketing+sidebar, `content` si doc+sidebar, `''` si home sans sidebar. `v2Sidebar()` retourne maintenant uniquement le contenu interne (div.sidebar-group), plus de `<aside>` wrapper (aligné sur sidebarFoundations/sidebarComponents). Gains WCAG : 5 pages gagnent `id="site-sidebar"`, `sidebar-overlay`, `sidebar-toggle` mobile (landmarks corrects, aria-controls, focus piégé). Règle de gouvernance ajoutée : `.claude/rules/layout-pattern.md`. 112 tests Playwright verts. |
| 2026-07-03 | refactor(site/layout): unification structure HTML sidebar — Pattern 1 et 2 fusionnés | **Cause : deux structures HTML distinctes** (`nav` vs `div.sidebar-group`) produisant deux rendus visuels différents (active state, padding, border-left). **Fix :** `v2Sidebar()` migré de `<nav>` vers `<div class="sidebar-group">` + état actif via `class="active" aria-current="page"` (même convention que Pattern 1). CSS Pattern 2 entièrement supprimé (`.with-sidebar .sidebar nav`, `.with-sidebar .sidebar a`, `:hover`, `:visited`, `[aria-current="page"]`) — les règles génériques `.sidebar a` et `.sidebar a.active` (border-left teal) s'appliquent désormais uniformément aux deux patterns. Seule la règle de grille `.with-sidebar{display:grid…}` conservée. 112 tests Playwright verts localement. |
| 2026-07-03 | fix(site/layout): sidebar scoping Pattern 2 — cascade CSS | **Règles CSS Pattern 2 scopées à `.with-sidebar .sidebar`**. Avant: `.sidebar nav` et `.sidebar a` sans scope écrasaient via cascade CSS (même spécificité, position plus tardive) les règles Pattern 1 `.sidebar>nav` et `.sidebar a{padding:6px 20px;border-left:2px solid transparent}`. Résultat: indicateur `border-left` actif de Pattern 1 (fondations/composants) était collé au bord du viewport (`padding:0.48rem 0.7rem` au lieu de `6px 20px`). Fix: toutes les règles Pattern 2 préfixées `.with-sidebar .sidebar` (spécificité 0,2,1 > Pattern 1's 0,1,1). Padding nav: `2rem 1.25rem` → `20px 0` (alignement cohérent top des deux patterns). Padding lien Pattern 2: `.48rem .7rem` → `.48rem 1.25rem` (compensation horizontal). Grid `.with-sidebar`: `220px` → `var(--agtc-site-sidebar-width,236px)` (largeur cohérente). |
| 2026-07-03 | fix(site/layout): sidebar flush header + fond pleine hauteur | **Deux patterns sidebar corrigés.** Pattern 1 (flex layout, pages fondations/composants) : `position:sticky;height/max-height;overflow-y` déplacés de `.sidebar` vers `.sidebar>nav,.sidebar>div`, et `align-self:stretch` ajouté sur `.sidebar` — le fond remplit maintenant toute la hauteur de page. Pattern 2 (grid `.with-sidebar`, pages docs marketing) : suppression du `padding-top:var(--agtc-header-height,64px)` de `.with-sidebar` (double offset avec `margin-top:64px` du `.home-layout`) et `min-height:calc(100svh - var(--agtc-header-height,64px))`. 41 snapshots visuels régénérés (macOS local → CI Linux requis via `update_snapshots`). |
|------------------|---------|---------|
| 2026-07-03 | feat(tests): items optionnels Playwright — axe-core, auto-issue, GitHub Pages | **3 items.** (1) axe-core fusionné dans Playwright : `tests/functional/accessibility.spec.js` — 20 tests (10 pages × light/dark), WCAG AA, 0 violation. `axe.yml` déprécié (manuel uniquement), `playwright.yml` rebaptisé "visuels, fonctionnels et accessibilité". (2) `playwright-reminder.yml` : crée automatiquement une GitHub Issue `visual-review` quand `components/` change sur `main` ; label créé automatiquement ; commentaire ajouté si une issue ouverte existe déjà. (3) `playwright.yml` : blob reporter + job `publish-report` fusionne les 3 browsers et publie sur GitHub Pages après chaque run. `How-to-designers.md` mis à jour avec l'URL Pages. |
| 2026-07-03 | feat(tests): Playwright — tests composants (approche A) | **92 tests, 92 verts.** Un spec fonctionnel par composant interactif : `button.spec.js` (7 tests — variants, disabled, icon-only, focus, clic), `segmented.spec.js` (6 tests — rendu shadow DOM, propriété `.value`, événement `change`), `tabs.spec.js` (6 tests — ARIA tablist/tab, switching, keyboard ArrowRight, slots nommés), `toggle.spec.js` (9 tests — checked, clic, agtc-change, disabled, a11y). Régressions visuelles : `all-components.spec.js` couvre 13 pages composants × light/dark = 26 snapshots. Corrections diagnostiquées : `agtc-segmented` émet `change` (pas `agtc-change`), `.value` est une propriété Lit sans `reflect` ; `agtc-tabs` utilise des slots nommés (pas la propriété `panel`) ; syntaxe TypeScript (`as any`, `as HTMLElement`) incompatible avec les fichiers `.js` Playwright. |
| 2026-07-02 | feat(tests): Playwright — infrastructure complète | **ADR-066 + 36 tests (21 fonctionnels + 15 visuels) tous verts.** Deux périmètres distincts documentés : (1) équipe DS → `tests/` dans ce dépôt, (2) équipes produit → leur propre dépôt (kit fourni dans `guidelines/foundations/testing.md`). Fichiers créés : `playwright.config.js`, `tests/visual/home.spec.js`, `tests/visual/documentation.spec.js`, `tests/visual/components/button.spec.js`, `tests/functional/nav.spec.js`, `tests/functional/sidebar.spec.js`, `.github/workflows/playwright.yml`, `.githooks/pre-push`. 15 snapshots PNG de référence générés dans `tests/visual/snapshots/`. `@playwright/test` ajouté aux devDependencies. `How-to-devs.md` et `How-to-designers.md` mis à jour. Chromatic conservé pour déclenchement manuel uniquement. |
| 2026-07-02 | fix(site/layout) | **Colonnes docs full-height + background page-content** — (1) `.with-sidebar > .sidebar` : `height:auto;position:static` annule le `height:calc(100vh-64px)` hérité du layout flex (ligne 827) qui bloquait `align-self:stretch` dans le contexte grid. (2) `.page-content` : `background:var(--agtc-semantic-color-background-page)` couvre le gradient aurora du body qui saignait à travers la colonne droite dans les zones sans contenu. |
| 2026-07-02 | fix(site/conformity) | **Dark mode — 5 valeurs hex en dur → tokens primitifs** — Bloc `[data-theme="dark"]` dans `site/build.js` : `#12a594` → `var(--agtc-primitive-color-teal-9)`, `#0d9b8a` → `var(--agtc-primitive-color-teal-10)`. 5 propriétés corrigées (`action-primary`, `action-primary-hover`, `button-primary-background`, `button-primary-background-hover`, `top-nav-tab-indicator-color`). Valeurs tracées dans `tokens/primitives.json` lignes 293-294. Conformité tokens : 100 %. |
| 2026-07-02 | fix(site/conformity) | **Audit conformité — corrections BLOQUANTES** — (1) `.pf-*` section Principe fondamental : 8 valeurs hex en dur (`#fff`, `#0a0c11`) → tokens `var(--agtc-semantic-color-text-on-dark)`, `var(--agtc-semantic-color-text-primary)`, etc. (2) `.brief-block` (bloc code IA) : 5 valeurs hex (`#111318`, `#1a1d26`, `#a4abb8`, `#d0d5e0`) → tokens `background-code`, `background-code-raised`, `text-on-code-*`. (3) `.human-control{background:#070910}` → `var(--agtc-semantic-color-background-inverse)`. (4) `.rd-doc-nav a:visited` : ajout fallback hex Safari (`#646464;var()`). (5) Mapping `contexts-utilisation.md` : 5 pages marketing ajoutées (pourquoi, architecture, qualite, ia, documentation). |
| 2026-07-02 | docs(how-to) | **Mise à jour How-to-devs.md + How-to-designers.md** — Devs : chemins `dist/css/` → `dist/tokens/css/`, suppression `.eslintrc-ds.json` (absent), `tcr/` → `token/` (git-workflow.md), `playwright` → axe-core CI, ajout `node site/build.js`, règle CSS naming (code-style.md). Designers : `session-spec.md` → `.claude/rules/` (routeur IA actuel). |
| 2026-07-02 | refactor(site) | **Ménage CSS/HTML — 10 inconsistances supprimées** — (1) S2 `.overlap` → `.overlap.overlap-reverse` : texte 40% au lieu de 60%. (2) `.ds-btn` 22 règles doublonnant `.cta-btn` supprimé + HTML migré. (3) `.illus-split` / `.illus-centered` règles orphelines supprimées. (4) `.sidebar-heading` dupliquait `.sidebar-label` — supprimé + HTML migré. (5) `.hero-actions` + `.hero-grid` définitions mortes supprimées, canonical consolidé avec `align-items:center`. (6) `.stat-num` (ligne 867) scopé en `.stat-band .stat-num` : évite conflit avec définition home. (7) `back-to-top back-to-top` → `back-to-top`. (8) Commentaire `ds-btn.on-dark` → `cta-btn-secondary`. |
| 2026-07-02 | fix(site/home) | **Layout colonnes texte-40%/image-60%** — (1) `.overlap` : `.56fr/.72fr` → `.6fr/.4fr`. (2) `.overlap-reverse` : `.62fr/.74fr` → `.4fr/.6fr`. (3) `.split` : `.86fr/.74fr` → `.6fr/.4fr`. (4) Hero : image 58% → 55%, texte `min(560px,50vw-2rem)` → `min(460px,40%)`. (5) S10 durabilité : restructuré de stacked (`.section-heading + .illus-centered`) vers `.overlap.overlap-reverse` côte à côte. (6) S8 mémoire des décisions : `.split` (copy first → image petite, pas de bleed) → `.overlap.overlap-reverse`. (7) IMG-CONTRACTS : même fix S8. |
| 2026-06-30 15:45 | refactor(css) | **Migration v2- complète — zéro préfixe de version** — 60 classes CSS + 13 custom properties + 1 animation renommées dans `site/build.js`. Mapping : `.v2-hero` → `.hero`, `.v2-nav` → `.site-nav`, `.v2-footer` → `.site-footer`, `.v2-shell` → `.shell`, `--v2-shell` → `--site-shell`, `--v2-teal` → `--site-teal`, `@keyframes v2-float` → `float-illus`. Règle documentée dans `.claude/rules/code-style.md` §"Nommage des classes CSS — règle absolue". Mémoire mise à jour. |
| 2026-06-30 15:30 | fix(site) | **Corrections home page** — (1) Retraite `width:50vw` des règles bleed : les figures restent dans leur colonne de grille, saignement uniquement vers l'extérieur. (2) `.copy` : `position:relative; z-index:1` — texte toujours au-dessus des figures. (3) `.human-control` → `background:#070910` (fond quasi-noir pour matcher le bg de IMG-HUMAN-LOOP.png). |
| 2026-06-30 15:20 | fix(site) | **Page /ai-brief — code block lisible** — `<agtc-code-block>` remplacé par `<pre>` natif + bouton Copier custom (le composant Lit ne supporte pas l'injection JS post-render dans un slot). CSS : `.brief-block`, `.brief-block-header`, `.brief-pre`. |
| 2026-06-30 14:40 | feat(site) | **Page `/ai-brief` + `context.md` téléchargeable** — `buildAiBrief()` dans `site/build.js`. Génère : `site/dist/ai-brief.html` (page démo, ~30 KB) + `site/dist/context.md` (~8 KB, brief structuré ~1800 mots). Contenu : identité, tokens 3 niveaux, 17 composants, accessibilité WCAG, gouvernance TCR, 2 contextes éditoriaux, 9 ADRs clés, permissions agents. Lien ajouté dans footer (colonne Explorer). |
| 2026-06-30 14:00 | Modifié | `.claude/rules/code-style.md` — ajout §"Nommage des classes CSS — règle absolue" : convention `site-`, `cta-btn`, `--site-*`, table de migration complète (ADR-2026-06-30). |
| 2026-05-28 00:00 | Créé    | `AGENTS.md` |
| 2026-05-28 00:00 | Créé    | `DESIGN.md` |
| 2026-05-28 00:00 | Créé    | `.claude/rules/project-overview.md` |
| 2026-05-28 00:00 | Créé    | `.claude/rules/tokens-system.md` |
| 2026-05-28 00:00 | Créé    | `.claude/rules/git-workflow.md` |
| 2026-05-28 00:00 | Créé    | `.claude/rules/development.md` |
| 2026-05-28 00:00 | Créé    | `.claude/rules/code-style.md` |
| 2026-05-28 00:00 | Créé    | `.claude/rules/components/button.md` |
| 2026-05-28 00:00 | Créé    | `decisions/ADR-001-trois-niveaux-tokens.md` |
| 2026-05-28 00:00 | Créé    | `decisions/ADR-002-lit-web-components.md` |
| 2026-05-28 00:00 | Créé    | `decisions/ADR-003-style-dictionary.md` |
| 2026-05-28 00:00 | Créé    | `decisions/ADR-004-gouvernance-humaine.md` |
| 2026-05-28 00:00 | Créé    | `decisions/ADR-005-variante-critical-vs-danger.md` |
| 2026-05-28 00:00 | Créé    | `decisions/ADR-006-chromatic-tests-visuels.md` |
| 2026-05-28 00:00 | Créé    | `decisions/ADR-007-axe-core-accessibilite.md` |
| 2026-05-28 00:00 | Créé    | `decisions/ADR-008-radix-colors.md` |
| 2026-05-28 00:00 | Créé    | `decisions/ADR-009-storybook.md` |
| 2026-05-28 00:00 | Créé    | `decisions/ADR-010-playwright.md` |
| 2026-05-28 00:00 | Créé    | `decisions/ADR-011-tokens-studio.md` |
| 2026-05-28 00:00 | Créé    | `decisions/ADR-012-audit-tokens-script.md` |
| 2026-05-28 00:00 | Créé    | `decisions/ADR-013-design-md-contrat-portable.md` |
| 2026-05-28 00:00 | Créé    | `decisions/ADR-014-conventional-commits.md` |
| 2026-05-28 00:00 | Créé    | `.claude/settings.json` |
| 2026-05-28 00:00 | Créé    | `decisions/ADR-015-hook-rappel-adr.md` |
| 2026-05-28 00:00 | Modifié | `decisions/README.md` |
| 2026-05-28 16:41 | Créé | `decisions/ADR-016-journal-construction.md` |
| 2026-05-28 16:41 | Modifié | `decisions/README.md` |
| 2026-05-28 23:27 | Créé | `decisions/ADR-017-correction-contraste-text-disabled.md` |
| 2026-05-28 23:55 | Créé | `decisions/ADR-018-migration-references-primitives-radix.md` |
| 2026-05-29 00:08 | Créé | `decisions/ADR-019-resolution-dynamique-tokens-build.md` |
| 2026-05-29 08:13 | Modifié | `AGENTS.md`, `DESIGN.md`, `README.md`, `How-to-designers.md`, `How-to-devs.md` |
| 2026-05-29 08:14 | Créé | `decisions/ADR-020-grille-4px.md` |
| 2026-05-29 08:16 | Créé | `decisions/ADR-021-atkinson-hyperlegible.md` |
| 2026-05-29 08:16 | Créé | `decisions/ADR-022-lucide-icons.md` |
| 2026-05-29 08:16 | Créé | `components/ds-icon.js`, `guidelines/components/icon.md` |
| 2026-05-29 09:13 | Créé | `.claude/instructions/session-spec.md` |
| 2026-05-29 09:16 | Modifié | `DESIGN.md` — nom système complet + note de réutilisation |
| 2026-05-29 09:30 | Modifié | `site/build.js`, tous fichiers sources — renommage "Agentic Design System" → "Système de design agentique" |
| 2026-05-29 09:45 | Modifié | `site/build.js`, `site/dist/` — remplacement de tous les emojis UI par des icônes Lucide SVG inline |
| 2026-05-29 09:32 | Modifié | `.claude/hooks/log-kit-construction.sh` |
| 2026-05-29 09:33 | Modifié | `.claude/hooks/log-kit-construction.sh` |
| 2026-05-29 10:13 | Créé | `.claude/instructions/site-checklist.md` |
| 2026-05-29 — | Modifié | `site/build.js`, `site/dist/` — traduction EN complète de toutes les pages internes (couleur, espacement, typo, icônes, composants, button, icon, tokens, décisions, agents, sidebars) |
| 2026-05-29 — | Modifié | `site/build.js`, `site/dist/site.js` — partage version EN via paramètre URL `?lang=en` (replaceState au clic) |
| 2026-05-29 — | Modifié | `site/build.js`, `site/dist/index.html` — correction lien GitHub : `gnegreiros/` → `gnegreiros-ux/agentic-design-system` |
| 2026-05-29 — | Modifié | `site/build.js`, `site/dist/site.css` — fix swatch couleur dans tableau tokens sémantiques : ajout `display:inline-block` |
| 2026-05-29 — | Modifié | `site/build.js`, `site/dist/site.css`, `site/dist/foundations/spacing.html` — preview espacement en liste verticale, suppression `border-top` du label |
| 2026-05-29 19:43 | Créé | `decisions/ADR-023-echelle-typographique-minor-third.md` |
| 2026-05-29 19:43 | Créé | `decisions/ADR-024-brand-palette-teal-accent-secondary.md` |
| 2026-05-29 19:44 | Créé | `decisions/ADR-025-densite-espacement-math-tokens.md` |
| 2026-05-29 19:44 | Modifié | `tokens/primitives.json` — palettes brand (accent, secondary), échelle typo Minor Third rem (xs→5xl), lineHeight (reading/heading/display), densité (normal/comfortable/compact) |
| 2026-05-29 19:44 | Modifié | `tokens/semantic.json` — action.primary → teal.9, border.focus → teal.9, nouveau groupe color.brand, typo étendue (detail/label/body/h5→h1/hero), espacement avec math tokens densité |
| 2026-05-29 19:54 | Créé | `decisions/ADR-026-sync-figma-palettes-custom.md` |
| 2026-05-29 19:54 | Modifié | `decisions/ADR-024-brand-palette-teal-accent-secondary.md` |
| 2026-05-29 20:13 | Créé | `decisions/ADR-025-densite-espacement-math-tokens.md` |
| 2026-05-29 20:33 | Modifié | `decisions/README.md` |
| 2026-05-30 — | Créé | `Prototype redesign site web système de design/` — prototype de redesign du site (index.html, color.html, site.css, site.js, tokens.css, images d'inspiration) |
| 2026-05-30 — | Modifié | `site/build.js`, `site/dist/foundations/typography.html` — échelle Minor Third (9 échelons xs→5xl, rem, line-height, poids), spécimens visuels, 3 modes lineHeight (reading/heading/display) |
| 2026-05-30 — | Modifié | `site/build.js`, `site/dist/foundations/spacing.html` — section densité math tokens (compact ×0.75, normal ×1.0, comfortable ×1.25), formule floor/ceil, ADR-025 |
| 2026-05-30 — | Modifié | `site/dist/` — rebuild complet, synchronisation ADR-023→026, tokens.css, couleur, index |
| 2026-05-30 10:54 | Créé | `.claude/skills/post-change-pipeline.md` |
| 2026-05-30 10:54 | Créé | `.claude/rules/post-change-pipeline.md` |
| 2026-05-30 10:56 | Créé | `decisions/ADR-027-pipeline-impact-pre-commit.md` |
| 2026-05-30 10:59 | Modifié | `decisions/README.md` |
| 2026-05-30 — | Modifié | `site/dist/` — rebuild, page ADR-027 générée (36 fichiers total)
| 2026-05-30 11:37 | Modifié | `DESIGN.md` |
| 2026-05-30 — | Modifié | `site/build.js`, `README.md`, `DESIGN.md` — changement de marque : "Système de design agentique" → **Agentica**, URL → designsystem.gnegreiros.com, hero + logo + meta mis à jour |
| 2026-05-30 — | Modifié | `site/dist/` — rebuild complet (36 fichiers), toutes les pages avec nouvelle identité Agentica
| 2026-05-30 — | Créé    | `Brand/` — dossier brand : logo SVG/PNG (5 variantes + symbole), favicons (ico, 16/32px, apple-touch, android-chrome), palette, color-pattern, image sociale |
| 2026-05-30 — | Modifié | `site/build.js`, `site/dist/` — logo SVG teal en header, favicons réels (Brand/Favicon/), webmanifest Agentica, métadonnées OG/Twitter optimisées (title 54 car, description 142 car, twitter:domain/url ajoutés) |
| 2026-05-30 17:58 | Créé | `decisions/ADR-028-atkinson-hyperlegible-mono.md` |
| 2026-05-30 17:58 | Modifié | `decisions/README.md` |
| 2026-05-30 18:12 | Créé | `.claude/skills/quality-gate.md` |
| 2026-05-30 18:12 | Créé | `.claude/skills/pipelines/tokens-audit.md` |
| 2026-05-30 18:13 | Créé | `.claude/skills/pipelines/wcag.md` |
| 2026-05-30 18:13 | Créé | `.claude/skills/pipelines/adr-conformity.md` |
| 2026-05-30 18:13 | Créé | `.claude/skills/pipelines/adr-triggers.md` |
| 2026-05-30 18:14 | Créé | `.claude/skills/pipelines/docs.md` |
| 2026-05-30 18:14 | Créé | `.claude/skills/pipelines/site.md` |
| 2026-05-30 18:14 | Créé | `.claude/skills/pipelines/commit.md` |
| 2026-05-30 18:14 | Créé | `.claude/skills/pipelines/style-dictionary.md` |
| 2026-05-30 18:15 | Créé | `.claude/skills/pipelines/storybook.md` |
| 2026-05-30 18:15 | Créé | `.claude/skills/pipelines/chromatic.md` |
| 2026-05-30 18:15 | Créé | `.claude/skills/pipelines/axe-core.md` |
| 2026-05-30 18:15 | Créé | `.claude/skills/pipelines/playwright.md` |
| 2026-05-30 18:15 | Créé | `decisions/ADR-029-quality-gate-pre-commit.md` |
| 2026-05-30 18:16 | Modifié | `decisions/README.md` |
| 2026-05-30 18:16 | Créé | `.claude/rules/post-change-pipeline.md` |
| 2026-05-30 — | Modifié | Tous les fichiers source — renommage sigle `sda` → `agtc`, préfixe CSS `--sda-` → `--agtc-`, balises WC `sda-icon/sda-button` → `agtc-icon/agtc-button`, localStorage `sda-lang` → `agtc-lang` |
| 2026-05-30 — | Créé | `package.json` — racine du monorepo, script `npm run tokens` |
| 2026-05-30 — | Créé | `.gitignore` — exclut `node_modules/`, `.tokens-build-tmp/` |
| 2026-05-30 — | Créé | `style-dictionary/build.js` — build SD avec parseur DTCG, transform `attribute/level`, formats custom Tailwind + M3 Angular |
| 2026-05-30 — | Modifié | `style-dictionary/config.json` — préfixe `sda` → `agtc`, ajout plateformes tailwind / angular / ios / android |
| 2026-05-30 — | Généré | `dist/tokens/css/` — primitives.css / semantic.css / components.css / all.css (--agtc- prefix, outputReferences) |
| 2026-05-30 — | Généré | `dist/tokens/js/tokens.js` — exports ES6 |
| 2026-05-30 — | Généré | `dist/tokens/tailwind/tokens.js` — extension theme.extend Tailwind |
| 2026-05-30 — | Généré | `dist/tokens/angular/_m3-theme.scss` — palettes M3 + alias SCSS |
| 2026-05-30 — | Généré | `dist/tokens/ios/AgenticaTokens.swift` — classe Swift |
| 2026-05-30 — | Généré | `dist/tokens/android/` — XML couleurs + dimensions |
| 2026-05-30 — | Créé | `decisions/ADR-030-style-dictionary-build-multi-platform.md` |
| 2026-05-30 — | Modifié | `decisions/README.md` — ajout ADR-030 |
| 2026-05-30 — | Créé | `components/agtc-button.js` — Web Component Lit, 4 variantes, confirmation critical 2-clics, loading width-stable |
| 2026-05-30 — | Modifié | `package.json` — ajout Lit (devDependency + peerDependency) |
| 2026-05-30 21:02 | Créé | `decisions/ADR-031-agtc-button-implementation.md` |
| 2026-05-30 21:02 | Modifié | `decisions/README.md` |
| 2026-05-30 21:09 | Modifié | `decisions/ADR-031-agtc-button-implementation.md` |
| 2026-05-30 21:09 | Modifié | `decisions/ADR-031-agtc-button-implementation.md` |
| 2026-05-30 21:09 | Modifié | `decisions/ADR-031-agtc-button-implementation.md` |
| 2026-05-30 21:19 | Modifié | `decisions/ADR-031-agtc-button-implementation.md` |
| 2026-05-30 21:19 | Modifié | `decisions/ADR-031-agtc-button-implementation.md` |
| 2026-05-30 — | Modifié | `components/agtc-button.js` — propriétés `icon` + `icon-suffix`, fallback slot, approche hybride |
| 2026-05-30 — | Modifié | `decisions/ADR-031-agtc-button-implementation.md` — décisions 10/11/12 : hybride, compat frameworks, icon-only |
| 2026-05-30 — | Modifié | `site/build.js` — démo icônes, tableau compat frameworks, CSS icon-only, rebuild 40 fichiers |
| 2026-05-30 22:39 | Créé | `decisions/ADR-032-storybook-stories-convention.md` |
| 2026-05-30 — | Phase 3 | **Storybook + Chromatic** — installation et configuration |
| 2026-05-30 — | Installé | `@storybook/web-components-vite` v10, `@storybook/addon-a11y`, `@chromatic-com/storybook` |
| 2026-05-30 — | Créé | `.storybook/main.js` — framework web-components-vite, stories colocalisées dans `components/` |
| 2026-05-30 — | Créé | `.storybook/preview.js` — tokens CSS Agentica injectés, addon-a11y en mode error |
| 2026-05-30 — | Créé | `components/agtc-button.stories.js` — 4 variantes × états + flux critical + icônes + AllVariants |
| 2026-05-30 — | Modifié | `package.json` — ajout `"type":"module"`, scripts `chromatic`, dépendance `lucide` |
| 2026-05-30 — | Créé | `vitest.config.js` — intégration Storybook + Vitest (via addon-vitest) |
| 2026-05-30 — | Mis à jour | `decisions/ADR-032-storybook-stories-convention.md` — convention stories complète |
| 2026-05-31 — | Créé | `decisions/ADR-033-agtc-input-implementation.md` |
| 2026-05-31 — | Créé | `components/agtc-input.js` — Web Component Lit, 7 types, label/helper/error, toggle password, icônes hybrides |
| 2026-05-31 — | Créé | `components/agtc-input.stories.js` — 8 états + types + icônes + AllStates |
| 2026-05-31 — | Créé | `decisions/ADR-034-agtc-badge-implementation.md` |
| 2026-05-31 — | Modifié | `tokens/component.json` — ajout tokens badge (6 variantes × 2 tailles, 26 tokens) |
| 2026-05-31 — | Rebuild | `dist/tokens/` — tokens badge compilés dans CSS + JS + Swift + Android |
| 2026-05-31 — | Fix | `style-dictionary/build.js` → `build.cjs` — CJS incompatible avec "type":"module" |
| 2026-05-31 — | Fix | `components/agtc-icon.js` — API Lucide v1.x (structure [[tag,attrs]]) |
| 2026-05-31 — | Créé | `components/agtc-badge.js` — 6 variantes, 2 tailles, icônes, icon-only |
| 2026-05-31 — | Créé | `components/agtc-badge.stories.js` — 9 stories + AllVariants |
| 2026-05-31 — | Modifié | `.storybook/preview.js` — import agtc-badge.js |
| 2026-05-31 — | Modifié | `tokens/component.json` — tokens card : elevated / flat / padding-none / sm / lg |
| 2026-05-31 — | Rebuild | `dist/tokens/` — 12 tokens card compilés |
| 2026-05-31 — | Créé | `components/agtc-card.js` — 3 variantes, 4 paddings, slots header/body/footer |
| 2026-05-31 — | Créé | `components/agtc-card.stories.js` — 8 stories + AllVariants + composition |
| 2026-05-31 — | Créé | `decisions/ADR-035-agtc-card-implementation.md` |
| 2026-05-31 — | Modifié | `.storybook/preview.js` — import agtc-card.js |
| 2026-05-31 17:02 | Créé | `decisions/ADR-035-agtc-card-implementation.md` |
| 2026-05-31 20:26 | Modifié | `.claude/settings.json` |
| 2026-05-31 20:29 | Modifié | `decisions/ADR-015-hook-rappel-adr.md` |
| 2026-05-31 20:29 | Modifié | `decisions/ADR-015-hook-rappel-adr.md` |
| 2026-05-31 22:50 | Créé | `.claude/rules/ux-patterns-sources.md` |
| 2026-05-31 22:51 | Créé | `.claude/skills/ux-pattern-review.md` |
| 2026-05-31 22:51 | Créé | `.claude/skills/pipelines/ux-patterns.md` |
| 2026-05-31 22:51 | Modifié | `.claude/skills/quality-gate.md` |
| 2026-05-31 22:51 | Modifié | `.claude/skills/quality-gate.md` |
| 2026-05-31 22:51 | Modifié | `.claude/rules/post-change-pipeline.md` |
| 2026-05-31 22:52 | Créé | `.claude/settings.json` |
| 2026-05-31 22:53 | Modifié | `.claude/settings.json` |
| 2026-05-31 22:54 | Créé | `decisions/ADR-036-ux-pattern-review-pre-composant.md` |
| 2026-05-31 22:54 | Modifié | `decisions/README.md` |
| 2026-05-31 22:54 | Modifié | `AGENTS.md` |
| 2026-06-01 08:27 | Modifié | `decisions/ADR-033-agtc-input-implementation.md` |
| 2026-06-01 08:27 | Revue UX | `input` — patterns NN/g+IxDF approuvés (tous), 6 surfaces documentées (ADR-036) |
| 2026-06-01 08:45 | Modifié | `decisions/ADR-022-lucide-icons.md` |
| 2026-06-01 08:45 | Revue UX | `icon` — patterns NN/g+IF approuvés (tous), 5 surfaces (pas de story) — ADR-036/022 |
| 2026-06-01 09:16 | Modifié | `decisions/ADR-031-agtc-button-implementation.md` |
| 2026-06-01 09:16 | Revue UX | `button` — patterns NN/g+IxDF+Smashing approuvés (tous, B4 reformulé), 6 surfaces (ADR-031/036) |
| 2026-06-01 09:22 | Modifié | `decisions/ADR-034-agtc-badge-implementation.md` |
| 2026-06-01 09:22 | Revue UX | `badge` — patterns NN/g+Dashboard approuvés (tous, BA1 = reco), 6 surfaces (ADR-034/036) |
| 2026-06-01 09:32 | Modifié | `decisions/ADR-035-agtc-card-implementation.md` |
| 2026-06-01 09:32 | Revue UX | `card` — patterns Dashboard+Smashing (C1/C3/C4 approuvés, C2 révisé anti-imbrication), 6 surfaces (ADR-035/036) |
| 2026-06-01 09:56 | Refacto | `site/build.js` — section Patterns UX lue depuis les guidelines md (source unique), fin de la duplication codée en dur |
| 2026-06-01 10:59 | Modifié | `.claude/skills/pipelines/chromatic.md` |
| 2026-06-01 10:59 | Modifié | `.claude/skills/pipelines/chromatic.md` |
| 2026-06-01 10:59 | Modifié | `.claude/skills/pipelines/chromatic.md` |
| 2026-06-01 10:59 | Modifié | `.claude/skills/quality-gate.md` |
| 2026-06-01 11:00 | Modifié | `decisions/ADR-006-chromatic-tests-visuels.md` |
| 2026-06-01 11:00 | Créé | `components/agtc-icon.stories.js` — 6ᵉ surface (Storybook) de la revue UX `icon`, complète enfin l'entrée du 08:45 |
| 2026-06-01 11:00 | Activation | Chromatic activé (ADR-006) — workflow CI `.github/workflows/chromatic.yml`, token sorti de `package.json` vers secret GitHub `CHROMATIC_PROJECT_TOKEN` (régénéré, ancien révoqué), pipeline + quality-gate → Actif |
| 2026-06-01 11:00 | Fix CI | `chromatic.yml` — `npm install` au lieu de `npm ci` (lockfile racine désync : arête `@emnapi/core` sans nœud) |
| 2026-06-01 11:00 | Maintenance | Workflows — `actions/checkout` et `actions/setup-node` v4 → v5 (Node 24, dépréciation Node 20 du 2026-06-16) |
| 2026-06-01 13:41 | Créé | `decisions/ADR-037-agtc-checkbox-implementation.md` |
| 2026-06-01 13:41 | Modifié | `decisions/README.md` |
| 2026-06-01 14:10 | Revue UX | `checkbox` — patterns NN/g+IxDF approuvés (CB1–CB7), forme **carrée** (écart assumé vs réf. ToDo ronde), `indeterminate` inclus — ADR-036/037 |
| 2026-06-01 14:10 | Composant | `agtc-checkbox` créé — 6 surfaces : tokens `component.checkbox`, code, story, guideline, ADR-037, site. WCAG 0 violation, Storybook OK. Seul composant DS manquant identifié à partir d'une réf. ToDo |
| 2026-06-01 19:19 | Créé | `decisions/ADR-038-agtc-radio-implementation.md` |
| 2026-06-01 19:19 | Créé | `decisions/ADR-039-agtc-toggle-implementation.md` |
| 2026-06-01 19:21 | Modifié | `decisions/README.md` |
| 2026-06-01 19:25 | Revue UX | `radio` — patterns NN/g+IxDF approuvés (R1–R7), forme **ronde**, architecture groupe+radio (exclusivité shadow DOM) — ADR-036/038 |
| 2026-06-01 19:25 | Composant | `agtc-radio` + `agtc-radio-group` créés — 6 surfaces : tokens `component.radio`, code (role=radiogroup, focus roving, clavier flèches), 2 stories, guideline, ADR-038, site |
| 2026-06-01 19:25 | Revue UX | `toggle` — patterns NN/g+IxDF approuvés (T1–T7), `role="switch"`, état par position seule (WCAG 1.4.1), track-off proxy gray.9 — ADR-036/039 |
| 2026-06-01 19:25 | Composant | `agtc-toggle` créé — 6 surfaces : tokens `component.toggle`, code (effet immédiat), story, guideline, ADR-039, site. WCAG 0 violation (57 pages), Storybook OK |
| 2026-06-01 20:51 | Modifié | `decisions/ADR-038-agtc-radio-implementation.md` |
| 2026-06-01 20:55 | Ajustement | `agtc-radio-group` — retrait de `Entrée` (garde `Espace` seul) pour coller au pattern WAI-ARIA strict ; 4 surfaces synchronisées (code, guideline, ADR-038, site) ; re-vérifié au navigateur |
| 2026-06-02 | Fix a11y | `agtc-checkbox` — ajout du garde `prefers-reduced-motion` (transitions `.box` désactivées) ; homogénéise les 3 contrôles de sélection (checkbox/radio/toggle) |
| 2026-06-02 | Fix site | `site/build.js` (`tokensCSS`) — émission des espacements primitifs `--agtc-primitive-space-N` manquants dans `tokens.css`. Corrige le padding nul des badges (et card sm/lg) qui référençaient ces vars indéfinies. Vérifié au navigateur (padding 4px/12px) |
| 2026-06-02 | Garde-fou | `site/build.js` (`validateCssVars`) — le build échoue désormais si une `var(--agtc-…)` sans fallback est référencée sans définition (anti-régression du bug ci-dessus). Exclut les exemples `<pre>`/`<code>`. Build : 580 définies · 95 référencées · 0 fantôme |
| 2026-06-03 19:57 | Créé | `decisions/ADR-040-agtc-table-implementation.md` |
| 2026-06-03 19:57 | Modifié | `decisions/README.md` |
| 2026-06-03 | Gap-analysis | UI du site → composants : cat. A (dogfooding : ds-btn→button, cards, badges), cat. B (manquants : table, code-block, banner, link, segmented), cat. C (chrome doc). Démarrage cat. B = `agtc-table` (692 usages `token-row`) |
| 2026-06-03 | Revue UX | `table` — patterns NN/g (Data Tables) + Smashing + Dashboard approuvés (T1–T10) ; séparateurs par défaut + `striped` option (T4) ; tri/filtre/pagination hors v1 mais **porte ouverte** (T10) — ADR-036/040 |
| 2026-06-03 | Composant | `agtc-table` créé — 6 surfaces : tokens `component.table`, code (mix : composant piloté par données shadow DOM + classe `.agtc-table` light DOM), story, guideline, ADR-040, site (page + nav + sidebar). Lecture seule, accessible (scope/caption/scroll). Vérifié au navigateur (site + shadow DOM, 0 erreur console). Build : 592 définies · 105 référencées · 0 fantôme |
| 2026-06-03 21:11 | Créé | `decisions/ADR-041-agtc-code-block-implementation.md` |
| 2026-06-03 21:11 | Modifié | `decisions/README.md` |
| 2026-06-03 | Revue UX | `code-block` — patterns DEV/whitep4nth3r + roboleary + Sara Soueidan (aria-live) + NN/g approuvés (CD1–CD9) ; coloration syntaxique (CD7) + numéros de ligne (CD9) hors v1, **porte ouverte** — ADR-036/041 |
| 2026-06-03 | Token | `primitive.fontFamily.mono` + `semantic.typography.mono.family` ajoutés (police monospace enfin tokenisée — applique ADR-028) ; `--agtc-font-mono` du site pointe désormais vers le token |
| 2026-06-03 | Composant | `agtc-code-block` créé — 6 surfaces : tokens `component.code-block` (surface sombre gray.12 tokenisée), code (mix : composant slotté shadow DOM + classe `.code-block`), story, guideline, ADR-041, site (page + nav + sidebar + `site.js` mis à niveau a11y : aria-label/focus/aria-live + label de langue). Lecture seule, copiable. Vérifié au navigateur (site + shadow DOM, copie annoncée, 0 erreur). Build : 604 définies · 125 référencées · 0 fantôme |
| 2026-06-03 21:57 | Créé | `decisions/ADR-042-agtc-banner-implementation.md` |
| 2026-06-03 21:57 | Modifié | `decisions/README.md` |
| 2026-06-03 | Revue UX | `banner` — patterns NN/g (Indicators/Notifications) + MDN (alert/status role) + A11Y Collective + Dashboard approuvés (N1–N9) ; statique par défaut (pas de live region), opt-in `live` ; sévérité jamais par la couleur seule — ADR-036/042 |
| 2026-06-03 | Composant | `agtc-banner` créé — 6 surfaces : tokens `component.banner` (6 variantes alignées badge, accent mutualisé bordure+icône), code (mix : composant slotté shadow DOM + classe `.agtc-banner`), story, guideline, ADR-042, site (page + nav + sidebar). Message inline, `dismissible` (événement `dismiss`), préfixe de sévérité masqué pour AT. Bug dismiss corrigé (masquage via `:host([hidden])`). Vérifié au navigateur (site + shadow DOM, dismiss OK). Build : 624 définies · 145 référencées · 0 fantôme |
| 2026-06-04 20:41 | Créé | `decisions/ADR-043-agtc-link-implementation.md` |
| 2026-06-04 20:41 | Modifié | `decisions/README.md` |
| 2026-06-04 | Revue UX | `link` — patterns NN/g (Visualizing Links) + WCAG H83 (nouvel onglet) approuvés (LK1–LK8) ; soulignement `always` par défaut (WCAG 1.4.1), externe = noopener + icône + texte AT, état visité hors v1 — ADR-036/043 |
| 2026-06-04 | Composant | `agtc-link` créé — 6 surfaces : tokens `component.link`, code (mix : composant shadow DOM + classe `.agtc-link`), story, guideline, ADR-043, site (page + nav + sidebar + utilitaire `.visually-hidden`). Lien de navigation, auto-détection externe, avertissement texte générique. Vérifié au navigateur (site + shadow DOM). Build : 627 définies · 148 référencées · 0 fantôme |
| 2026-06-04 21:02 | Créé | `decisions/ADR-044-agtc-segmented-implementation.md` |
| 2026-06-04 21:02 | Modifié | `decisions/README.md` |
| 2026-06-04 | Revue UX | `segmented` — patterns Primer (Segmented Control) + NN/g + WCAG 1.4.1 approuvés (SG1–SG8) ; groupe de boutons + aria-current + effet immédiat (écart assumé vs radiogroup), navigation Tab native — ADR-036/044 |
| 2026-06-04 | Composant | `agtc-segmented` créé — 6 surfaces : tokens `component.segmented`, code (mix : composant piloté par données shadow DOM + classe `.agtc-segmented`), story, guideline, ADR-044, site (page + nav + sidebar). Mono-sélection effet immédiat, émet `change`. Vérifié au navigateur (site + shadow DOM : clic → aria-current + change émis). **Clôt la catégorie B** de la gap-analysis (table, code-block, banner, link, segmented). Build : 634 définies · 155 référencées · 0 fantôme |
| 2026-06-04 | Dogfooding | Cat. A — `contributionBanner()` migré : consomme `.agtc-banner brand` + `.agtc-link` (ADR-042/043), HTML/CSS bespoke `.contribution-banner` supprimé. Fix robustesse de la classe banner (`.banner-content span` → `> span` : évite que le corps écrase la couleur du titre/lien imbriqués). Refactor (pas de nouvel ADR). Vérifié au navigateur. Build : 634 · 155 · 0 fantôme |
| 2026-06-04 | Dogfooding | Cat. A — bascule de langue du header migrée : .lang-toggle-group/.lang-btn vers .agtc-segmented .lang-switch (ADR-044) + aria-current. site.js mis a jour (selecteur .lang-switch button, exclut <html data-lang> et les demos). CSS bespoke supprime (override compact conserve, pas de taille sm). Verifie : bascule FR/EN OK, demos non affectees. Build : 634 - 0 fantome |
| 2026-06-04 | Dogfooding | Cat. A — renommage `.ds-btn` vers `.agtc-button` (CSS + ~22 usages). La classe consommait deja les tokens `--agtc-component-button-*` : pur renommage, ZERO changement visuel, elimine la dette du prefixe legacy `ds-` (reperee a la gap-analysis). Verifie au navigateur (4 variantes identiques). Build : 634 - 0 fantome |
| 2026-06-04 | Dogfooding | Cat. A — badge de statut migre : `.badge`/`.badge-active` (couleurs EN DUR #ecfdf5/#18794e) -> classe `.agtc-badge success sm` tokenisee (6 variantes, consomme `--agtc-component-badge-*`, ADR-034). Corrige une vraie dette de tokens + dogfoode le badge. 2 usages (index decisions + meta ADR). Verifie au navigateur (44 badges, green.3/green.11). Note : #ecfdf5/#18794e subsistent dans .rule-can/.audit-* (dette separee). Build : 634 - 0 fantome |
| 2026-06-04 | Dogfooding | Cat. A — tables du site : le style de base `table`/`th`/`td`/`tr:hover` route desormais ses COULEURS via `component.table.*` (ADR-040) au lieu des semantiques directes. Le site s aligne sur le contrat du composant (principe : le site consomme le DS). Seul changement visuel : corps des tables text.secondary -> text.primary (gris.12, plus lisible). 27 token-tables + toutes les autres, zero changement markup. Verifie au navigateur. Build : 634 - 0 fantome |
| 2026-06-04 | Dogfooding | Cat. A — liens de contenu : le style de base `a` route sa couleur via `component.link.*` (ADR-043) + ajout du `a:hover` du contrat composant (teal.9 -> teal.10). Le site suit le composant. Couleur de base inchangee (meme valeur), hover ajoute. Verifie au navigateur. Build : 634 - 0 fantome |
| 2026-06-04 | Dogfooding | Cat. A — cartes-grilles : surface (fond/bordure/rayon) de `.nav-card`/`.tool-card`/`.step-card`/`.principle-card` routee via `component.card.*` (ADR-035). Layout interne + hover restent propres au site. Affichages de chiffres (kpi/stat) laisses hors card (recherche : Polaris Box, Curtis composabilite). Valeurs identiques -> zero changement visuel. Verifie au navigateur. Build : 634 - 0 fantome |
| 2026-06-04 | Dette/tokens | Tokenisation des couleurs de TEXTE en dur (correspondances exactes) : `.logo-name`/`.hero-name` #12A594 -> brand.primary ; `.icon-no` + audit fails #ce2c31 -> feedback.danger ; `.icon-ok` #1a7f37 + audit pass #18794e -> feedback.success. Cible via prefixe `color:` (epargne SVG logo/manifest/affichage token). Verifie navigateur. RESTE (dette gouvernee, nouveaux tokens requis) : fonds/bordures subtils success/danger + palette warning (.rule-can/.do-section/.audit-*). Build : 634 - 0 fantome |
| 2026-06-05 09:00 | Créé | `decisions/ADR-045-feedback-color-family-completion.md` |
| 2026-06-05 09:00 | Modifié | `decisions/README.md` |
| 2026-06-05 | Dette/tokens | Solde de la dette du 2026-06-04 : famille `semantic.color.feedback` complétée (8 tokens : `danger-border`, `success-subtle/border`, `warning`+`-subtle`/`-border`, `info-subtle/border`) — symétrie 4 sévérités × 3 rôles (texte `-11` / fond `-3` / bordure `-6`, convention Radix). `site/build.js` route désormais `.rule-can/.rule-cannot`, `.do-section/.dont-section`, `.audit-badge`, `.audit-card--*` via le sémantique (zéro feedback en dur). ADR-045. WCAG : warning `#cc4e00` sur blanc = 4.51:1 (AA) ; bordures `-6` non textuelles (exemptes). Vérifié au navigateur (DO green.3/6, DON'T red.3/6, warning orange.11). Build : 642 défini · 160 référencé · 0 fantôme |
| 2026-06-05 | Dogfooding | Infobox de doc ajoutée sur l'accueil (sous le pipeline de tokens) : nouvelle classe site `.infobox` consommant `feedback.info-subtle` (fond) + `feedback.info-border` (bordure) + `feedback.info` (icône) — solde les 2 derniers tokens de la famille ajoutés par symétrie à l'ADR-045. Contenu : rappel de la règle absolue « jamais de primitif dans un composant ». WCAG : texte gray.12 sur blue.3 = 14.5:1 (AA) ; lien blue.11 sur blue.3 écarté (4.25:1 < 4.5) → lien text.primary + soulignement (affordance non par la couleur seule, WCAG 1.4.1) ; icône décorative `aria-hidden`. `role="note"`. Vérifié au navigateur. Build : 642 défini · 162 référencé · 0 fantôme |
| 2026-06-05 09:30 | Créé | `decisions/ADR-046-inverse-surfaces-shadows-tokens.md` |
| 2026-06-05 09:30 | Modifié | `decisions/README.md` |
| 2026-06-05 | Dette/tokens | Surfaces inversées + ombres + jetons décoratifs (ADR-046). Découverte : les échelles primitives `white.*`/`black.*` (alpha) existaient mais étaient orphelines. Ajout : 2 primitifs neutres (`neutral.900` #1a1e24, `neutral.950` #0f1117) + 11 sémantiques (`background.inverse`/`-raised`, `text.on-inverse`/`-secondary`/`-muted`, `border.on-inverse`/`swatch`, `viz.scale-bar`, `shadow.header`/`raised`/`card-hover`). Migration `site/build.js` : bandeau stats, pied de page, en-tête, nav mobile, tooltips palette, hover nav-card, échantillons, barre d'espacement. **Correctif WCAG** : pied de page (.35) + lien audit (.30) échouaient AA (~3:1) → `on-inverse-muted` (.52 = 5.69:1) — auto-guérison encadrée. Résidu assumé : démo `toggle` (#8d8d8d=gray.9 + ombre bouton) annotée « proxy », reportée (dogfooding de démos). Exempts : logo SVG, affichages de valeurs de jetons, exemples de code, manifest. Vérifié au navigateur (pied de page + stats sombres). Build : 655 défini · 173 référencé · 0 fantôme |
| 2026-06-05 | Dogfooding | Résidu ADR-046 soldé — démo `toggle` (`buildToggle()`) consomme désormais son contrat : ajout des 7 jetons `toggle-default-*` à la map COMP du site (miroir fidèle de `component.json`, incl. `track-off`→gray.9 comme badge-info→blue-3) ; la démo route `track-off`/`track-on`/`knob` via `--agtc-component-toggle-*` (fin du proxy `#8d8d8d`). Annotations « (proxy) » retirées des tables de jetons. Ombre du curseur laissée en dur : reflète fidèlement le composant `agtc-toggle` (délimiteur WCAG 1.4.11, non tokenisé dans le contrat). ZÉRO changement visuel (gray.9 = #8d8d8d exact). Refactor (pas de nouveau jeton, pas d'ADR). Vérifié au navigateur (OFF gris, ON teal). Build : 662 défini · 177 référencé · 0 fantôme |
| 2026-06-05 | Site/feat | Page **Démarrer / Get started** ajoutée (`buildGetStarted()`) — chemin d'adoption en 3 étapes (récupérer les tokens compilés → importer/consommer les variables CSS par intention → monter les Web Components `agtc-*`), tableau des 6 plateformes de sortie, section « Pour les agents IA ». Sert les 3 objectifs du site : promouvoir, documenter, exemple vivant (bâtie 100 % avec les composants dogfoodés : `agtc-banner` info/brand, `code-block`, `info-card`/`grid-3`, `table`, `agtc-button`). Nav header + CTA hero d'accueil pointent désormais vers `get-started.html` (auparavant « Explorer les fondations »). Bandeau pré-version assumé : npm à venir, consommation actuelle via le dépôt. ZÉRO valeur en dur (seul littéral : URL du dépôt). Pas de nouveau jeton, pas de composant → pas d'ADR. Build : 662 défini · 177 référencé · 0 fantôme · 64 fichiers. |
| 2026-06-05 09:45 | Créé | `decisions/ADR-047-no-visited-nav-rule.md` · `.claude/rules/no-visited-nav.md` |
| 2026-06-05 09:45 | Créé | `decisions/ADR-048-action-teal-wcag-contrast.md` |
| 2026-06-05 09:45 | Modifié | `decisions/README.md` (ADR-047, ADR-048) |
| 2026-06-05 | Redesign/Phase A | Corrections rapides du site (brief redesign). **A1** : « Démarrer » placé en 1er dans la nav header + traité comme CTA rempli (`.nav-cta`, action-primary, prioritaire même en état actif). **A2** : en-têtes `.token-table th` ne brisent plus les mots (`word-break:normal;hyphens:none`). **A4** : icon-button **Storybook** (`book-open`) ajouté au header (avant GitHub) et au footer, pointant le Storybook publié par Chromatic (`main--6a1c1e665ec5fe8fc0540983.chromatic.com`, HTTP 200 vérifié). Vérifié au navigateur (header + hero). Build : 662 · 0 fantôme. |
| 2026-06-05 | Redesign/Phase B + a11y | **Règle système `no-visited-nav`** (ADR-047) : les éléments de navigation ne portent jamais d'état `:visited` distinct — portée système entière (site + composants). Bloc CSS consolidé neutralisant `:visited` sur 6 surfaces (top-nav, sidebar, toc, footer-links, audit-footer-link, boutons-icônes) via tokens sémantiques, déclaré avant `:hover`/`.active`. Remplace les déclarations dispersées préexistantes. |
| 2026-06-05 | token(semantic)/a11y | **Correctif WCAG AA — teal interactif** (ADR-048). Découverte au redesign : `action.primary`=teal.9 (#12a594) donne **blanc/teal.9 = 3.07:1** (bouton primaire) et **teal.9 texte/blanc = 3.07:1** (liens actifs) → échec AA 4.5:1, sur TOUT le système (CTA hero inclus). L'ADR-024 affirmait à tort « teal.9 = 4.6:1 » (c'est teal.11). Fix : `action.primary` teal.9→**teal.11** (#008573, **4.56:1** ✅), `action.primary-hover` teal.10→**teal.12** (12:1), `border.focus` teal.9→teal.11 (unifié). `brand.primary` **reste teal.9** (identité = logo SVG/manifest, logotype exempt) → séparation teal d'identité / teal d'action. Affichage de code du token mis à jour (#12A594→#008573). Auto-guérison encadrée, approuvée. Vérifié au navigateur. Build : 662 défini · 177 référencé · 0 fantôme · 66 fichiers. |
| 2026-06-05 10:00 | Créé | `decisions/ADR-049-card-shadow-token.md` · Modifié `decisions/README.md` |
| 2026-06-05 | Redesign/Phase C + token | **Solde du résidu d'ADR-046** (dernière ombre brute du système). Ajout du sémantique `shadow.card` (`0 1px 3px rgba(0,0,0,.10), 0 1px 2px rgba(0,0,0,.06)` — ombre de carte au repos) ; `component.card.elevated.shadow` re-pointé de la valeur brute vers `{semantic.shadow.card}`. **Valeur résolue identique → ZÉRO changement visuel** (pur refactor d'indirection). Miroir COMP `site/build.js` + ligne d'affichage doc carte mis à jour. Gouvernance : 1 sémantique (Design System Lead) + 1 jeton de composant re-pointé (Principal Designer). ADR-049. Build : 663 défini · 178 référencé · 0 fantôme · 67 fichiers. |
| 2026-06-06 | Test/a11y | **Gate axe-core activé en mode rapport** (applique ADR-007). `scripts/axe-audit.js` (Playwright + @axe-core/playwright) scanne les 73 pages de `site/dist/`, exclut le logotype `.logo`/`.hero-name` (exempt WCAG 1.4.3), exit 1 sur `critical`/`serious` sauf `AXE_BLOCKING=false`. Workflow `.github/workflows/axe.yml` (build + audit + artefact). `@axe-core/playwright` ajouté aux devDeps (lockfile non régénéré localement, cf CI). `axe-report.json` gitignoré. Pipeline doc → ✅ Actif (mode rapport). **Burn-down découvert : 76 violations.** Cause racine dominante : `action.primary` teal.11 (#008573) en texte sur fond de page `#fcfcfc` = **4.44:1** (échec AA d'un cheveu, uniforme : boutons secondary/ghost, nav active, code, liens) — pas de palier teal entre 11 et 12 → nécessite un jeton d'action un poil plus sombre (décision Principal Designer). Plus `aria-prohibited-attr` (div décoratifs) + `label` ×2. Bascule en bloquant après résorption. |
| 2026-06-06 11:14 | Modifié | `.claude/skills/pipelines/axe-core.md` |
| 2026-06-06 11:14 | Modifié | `.claude/skills/pipelines/axe-core.md` |
| 2026-06-06 11:15 | Créé | `decisions/ADR-050-action-teal-contrast-real-surface.md` |
| 2026-06-06 | token(primitif)/a11y | **Burn-down WCAG — teal d'action sur la surface réelle** (ADR-050, corrige ADR-048). L'ADR-048 certifiait `action.primary`=teal.11 à 4.56:1 mais **sur blanc pur** ; le fond de page réel est `gray.1` **#fcfcfc**, où teal.11 `#008573` en texte = **4.45:1** → échec AA d'un cheveu, uniforme (nav active, code, liens, texte boutons secondary/ghost). Pas de palier Radix entre teal.11 et teal.12 (quasi-noir). **Fix : retune primitif `teal.11` #008573 → #007a68** (seuls consommateurs : `action.primary` + `border.focus`) = **5.14:1** en texte sur #fcfcfc, **5.27:1** texte blanc sur bouton (WCAG AA, ~0.6 de marge). Hover teal.12 et brand teal.9 inchangés. Intentions sémantiques + affichage de code (`site/build.js`) + doc pipeline `axe-core.md` mis à jour. **axe : 76 → 0 violations** sur 74 pages. Résidus traités dans la même passe : `aria-prohibited-attr` (swatches couleur/espacement) → `role="img"` ; `label` (inputs de démo) → association `for`/`id` ; `color-contrast` résiduel (libellés de démo **désactivés**, exempt WCAG 1.4.3) → `aria-disabled="true"` sur la ligne désactivée (checkbox + radio). Le gate axe-core peut désormais basculer en **bloquant** (retirer `AXE_BLOCKING:'false'`). Gouvernance : valeur de jeton primitif — approbation Principal Designer. Build : 663 défini · 178 référencé · 0 fantôme · 68 fichiers. |
| 2026-06-06 12:02 | Modifié | `.claude/skills/pipelines/axe-core.md` |
| 2026-06-06 14:00 | Créé | `decisions/ADR-051-illustration-style-palette.md` |
| 2026-06-06 14:01 | Modifié | `.claude/rules/tokens-system.md` |
| 2026-06-06 14:13 | Modifié | `decisions/README.md` |
| 2026-06-06 14:25 | Modifié | `DESIGN.md` |
| 2026-06-06 | Standards | **Adoption explicite du standard W3C DTCG** (designtokens.org) comme référence du format des design tokens. Propagation multi-surfaces : (1) `.claude/rules/tokens-system.md` — nouvelle section « Standard de référence » + tableau des conventions DTCG (`$value`/`$type`/`$description`/alias/`$schema`), règle « le standard fait foi » ; (2) `DESIGN.md` §2 — sous-section « Conformité au standard (W3C DTCG) » ; (3) `tokens/primitives.json` — ajout `$schema` (alignement avec `semantic.json` + `component.json` qui le portaient déjà → 3/3 fichiers conformes) ; (4) **site** — section d'accueil « Standards ouverts » avec **logo officiel DTCG inline** (fill `currentColor` = `text.primary`, lien externe `noopener` + `aria-label`, SVG `aria-hidden`+`<title>`) déclarant « Agentica suit le standard DTCG » (FR/EN) ; (5) `site/contenu.md` — puce « Standards ouverts » FR + EN (source de vérité copy) ; (6) logo officiel archivé `Brand/standards/dtcg-logo.svg` (versionné hors dossier prototype). Aucune valeur de token modifiée (ajout `$schema` = métadonnée DTCG, pas un changement de contrat). Build : 668 défini · 178 référencé · 0 fantôme · 69 fichiers. Audit axe non lancé en local (navigateurs Playwright absents → tourne en CI) ; combos de tokens employés déjà audités. |
| 2026-06-06 14:39 | Créé | `decisions/ADR-052-dtcg-standard-conformance.md` |
| 2026-06-06 14:40 | Modifié | `decisions/README.md` |
| 2026-06-06 14:40 | Modifié | `.claude/rules/tokens-system.md` |
| 2026-06-06 14:40 | Modifié | `DESIGN.md` |
| 2026-06-06 | Site/feat | **Logos d'intégration vendor sur le site** (suite décision DTCG). Les vrais logos de marque remplacent les icônes Lucide génériques dans les cartes/tables existantes : Storybook (tool-card « Stack technique » accueil), Tailwind/Angular/Swift/Android (table « Plateformes de sortie » get-started), React ×2/Angular (table « Compatibilité frameworks » page button). CSS/JS, HTML/Vue/Svelte gardent l'icône Lucide `code` (pas de logo dédié). Logos officiels archivés/versionnés `Brand/integrations/{react,angular,swift,android,tailwind,storybook}.svg`, copiés vers `site/dist/integrations/` par `build.js`, servis via `<img>` en couleurs de marque (pas de tokenisation — actifs tiers). Tous avec `alt` + `loading="lazy"`. DTCG reste en déclaration « Standards ouverts » (inline, `currentColor`). Aucun token modifié, pas d'ADR (ajout d'actifs illustratifs sur pages existantes, lib d'icônes Lucide inchangée). Build : 668 défini · 178 référencé · 0 fantôme. |
| 2026-06-06 15:41 | Créé | `Brand/integrations/` — sources brutes des logos d'intégration (react, angular, swift, android, tailwind, storybook SVG) |
| 2026-06-06 15:55 | Créé | `illustrations/` — 3 illustrations v1 style Tactile Tech + dossier inspiration (ADR-051) : `pipeline-tokens.svg`, `human-last-word.svg`, `multi-platform.svg` |
| 2026-06-06 15:59 | Approuvé | Illustrations v1 marquées comme approuvées dans `guidelines/foundations/illustration.md` (décision humaine) |
| 2026-06-06 16:12 | Site/feat | **Intégration des 3 illustrations SVG inline sur le site.** `pipeline-tokens.svg` avant la section `.pipeline` des tokens, `human-last-word.svg` après la grille des valeurs non négociables, `multi-platform.svg` avant le stack-flow. CSS `.illus-block` : border-radius 12px, overflow hidden, svg 100% responsive. `role="img"` + `aria-label` sur chaque SVG inline. |
| 2026-06-06 16:12 | Site/feat | **5 corrections header/nav/sections.** Sections « Valeurs non négociables », « Pipeline de tokens » et « Stack technique » enveloppées dans `.home-section-ink` (fond `illustration-ink` pleine largeur, textes inverse). Logo Storybook officiel inline SVG remplace `icon('book-open')` dans header et footer. Navigation unifiée : règles hover/active séparées, CTA « Démarrer » repositionné. `<meta name="author">` ajouté dans les 76 HTML. |
| 2026-06-06 17:15 | fix(site) | **3 corrections sections sombres, visited state, active nav.** Sections dark : `h2` sans margin-top/padding-top hérités (168px supprimés) ; paragraphes en `text-on-inverse-secondary` (0.75). Visited nav : hex fallback explicite avant `var()` (Safari). Active nav : refactorisation JS — `hDir`/`hFile` au lieu de `p.includes('index')`, un seul lien actif par page. |
| 2026-06-06 19:02 | a11y(illustrations) | **Hausser opacités texte SVG — contraste WCAG AA.** Titres (0.32→1.0), captions (0.22→0.70), textes secondaires (0.28-0.42→0.65-0.70) sur les 3 SVG. Tous les textes porteurs de sens passent 4.5:1 sur fond ink `#211f26`. |
| 2026-06-06 19:43 | Site/feat | **Fond SVG → section CSS, cards dark-glass, `[data-theme=light]` prêt.** Suppression du `<rect>` de fond de chaque SVG — background fourni par `.home-section-ink` (source unique). `principle-cards` en verre sombre (`rgba 6%`) dans le contexte ink. Bloc `[data-theme="light"]` complet préparé (section subtle, cards blanches, texte SVG adaptatif). |
| 2026-06-06 19:58 | Modifié | `AGENTS.md` — mise à jour Agent skills config (GitHub Issues, triage labels, domain docs) |
| 2026-06-06 | style(illustrations) | **Tokenisation couleurs + ménage des exports Figma (3 SVG).** Remplacement des 5 couleurs d'illustration tokenisables par leurs variables CSS sémantiques : `#12A594`→`illustration-brand`, `#E93D82`→`illustration-accent`, `#8B9098`→`illustration-neutral`, `#EFF1F3`→`illustration-surface`, `#1E1B22`→`illustration-ink`. Couleurs conservées en dur : `#1A1820` (panneau interne, pas de token exact), vendor colors (`#1572B6`,`#F05138`,`#DD0031`,`#3DDC84`,`#38BDF8`,`#F7DF1E`), couleurs introduites par l'utilisateur dans Figma sans correspondance de token (`#B0B6B9`,`#291741`,`#1C2E32`,`#A8B5BE`,`#0F8A7C`). Suppression du wrapper `<g clip-path>` + `<defs><clipPath>` redondants générés par Figma (clip = viewport entier `800×420`). Zéro régression visuelle (screenshots prod). |
| 2026-06-06 | style(illustrations) | **Police Atkinson Hyperlegible dans les 3 illustrations SVG.** Remplacement de `system-ui,sans-serif` → `'Atkinson Hyperlegible', system-ui, sans-serif` et `'Courier New',monospace` → `'Atkinson Hyperlegible Mono', 'Courier New', monospace` dans `illustrations/pipeline-tokens.svg`, `human-last-word.svg`, `multi-platform.svg`. Ajout d'un bloc `<style>@import url(Google Fonts)</style>` dans chaque `<defs>` pour le rendu standalone/Figma. Le site charge déjà la police via CSS (ligne 285 build.js), les SVG inline héritent du contexte page. Aussi : résolution de la CSS var `fill="var(--agtc-semantic-color-illustration-brand,#12a594)"` → `fill="#12a594"` dans pipeline-tokens.svg (Figma ne résout pas les CSS vars dans les attributs SVG). Vérifié au navigateur (3 screenshots Playwright, font chargée). |
| 2026-06-06 | fix(site) | **Sections illustration toujours sombres en thème light** (Option A — décision humaine). Les 4 règles `[data-theme="light"] .home-section-ink` qui changeaient le background en `background.subtle` et forçaient `svg text { fill: text-primary }` ont été retirées de `site/build.js`. Seules les `principle-card` à l'intérieur de ces sections s'adaptent au light (overrides conservés). Les SVG à l'intérieur des `illus-block` restent dark avec textes `#eff1f3` lisibles dans les deux thèmes. Motif : les 3 SVG contiennent des panneaux internes `fill="#1e1b22"` — forcer le texte en dark y créait du texte sombre sur fond sombre (contraste insuffisant). Vérifié au navigateur (3 screenshots Playwright, thème light). Build : 668 défini · 178 référencé · 0 fantôme · 70 fichiers. |
| 2026-06-07 11:11 | style(illustrations) | **Tokenisation couleurs résiduelles WCAG AA (3 SVG).** Remplacement des 6 couleurs sans token par leurs équivalents sémantiques les plus proches passant les tests de contraste : `#1A1820`→`background-inverse-raised`, `#0F8A7C`→`brand-primary-hover` (5.08:1 non-texte), `#291741`→`illustration-ink` (opacity 0.41), `#1C2E32`→`background-inverse-raised`, `#B0B6B9`→`text-on-inverse-secondary` (8.56:1), `#A8B5BE`→`text-on-inverse-secondary` (8.38:1). Vendor colors (CSS/Swift/Angular/JS/Tailwind/Android) conservées en dur (`multi-platform.svg`). |
| 2026-06-07 13:39 | fix(site) | **Marges illustrations + logos frameworks page Button.** Ajout `margin-top:64px` / `margin-bottom:48px` sur `.home-section-ink .illus-block` (desktop + mobile). Correction des chemins `src="integrations/*.svg"` → `src="../integrations/*.svg"` dans la table Compatibilité frameworks de la page Button (profondeur 1). |
| 2026-06-07 14:16 | fix(site) | **Logos frameworks + marges illustrations.** Suppression `width/height="800px"` dans react/angular/storybook/tailwind SVG (viewBox suffit). Ajout `vue.svg` et `svelte.svg` officiels. Remplacement des icônes Lucide génériques par les vrais logos Vue/Svelte dans la table Compatibilité frameworks. Marge `margin-top:64px margin-bottom:0`. |
| 2026-06-07 17:49 | token(semantic) | **Conformité DTCG complète + tokens composites Figma.** Migration `semantic.json` et `component.json` de `value` → `$value` (standard W3C DTCG). Correction `flattenTokens` dans `site/build.js` pour lire `$value` en priorité. Ajout `tokens/figma.json` (tokens composites typographie pour Tokens Studio). Rebuild dist/ + site/dist/ — 0 variable fantôme. |
| 2026-06-07 18:28 | Créé | `tokens/$metadata.json` — fichier requis par Tokens Studio pour la découverte multi-fichier. `tokenSetOrder: primitives → semantic → component → figma`. Sans ce fichier, Tokens Studio ignorait `tokens/figma.json` et ne générait aucun Text Style. |
| 2026-06-08 11:04 | Créé | `docs/demarche-agentica.md` — document de démarche Agentica destiné aux non-techniques (designers, conseillers CX, gestionnaires). Explique la construction du système en 5 étapes avec schémas ASCII. |
| 2026-06-10 | Session | **Session corrections site 10-06-2026 — 9 tranches livrées.** Voir détail ci-dessous. |
| 2026-06-10 19:40 | Site/feat | **Bouton retour en haut + résolution conflit sidebar/TOC.** Bouton flottant « Retour en haut » sur toutes les pages (apparaît après 25% de scroll, smooth scroll, 44px touch target, WCAG 2.5.8). Suppression de la sidebar page Agents (ancres in-page = doublon TOC résolu). Retrait des ancres in-page de la sidebar Tokens (TOC prend en charge la navigation). |
| 2026-06-10 19:50 | Site/feat | **Page « Vue d'ensemble Fondations » ajoutée** + lien nav corrigé. Hub des fondations accessible depuis la sidebar et la nav principale. |
| 2026-06-10 20:03 | fix(site) | **Tables : alignement + largeurs colonnes + Storybook dans la stack.** Colonnes fixes (`white-space:nowrap` ou largeurs explicites) sur les tables récurrentes. Carte Storybook ajoutée dans la section Stack technique de l'accueil. |
| 2026-06-10 21:14 | fix(ui) | **Suppression des FULL CAPS** dans la sidebar, le footer et les logos stack. Passage en casse naturelle + capitalize CSS là où pertinent. |
| 2026-06-10 21:28 | Site/feat | **DTCG en haut + icon button avec variantes + page Changelog v0.1.0.** Section DTCG remontée dans la hiérarchie d'accueil. Documentation de l'icon button enrichie (variantes inline). Page Changelog créée pour la version v0.1.0. |
| 2026-06-10 22:10 | fix(tokens) | **Filtre tokens fiable + sections vides masquées + compteur de résultats.** Debounce sur le filtre, masquage des sections sans résultat correspondant, compteur « N token(s) trouvé(s) » accessible. |
| 2026-06-10 22:24 | fix(mobile) | **Sidebar après le contenu + hamburger dans l'écran.** Correction de l'ordre DOM : sidebar déplacée après le `<main>` pour que le lecteur d'écran lise le contenu principal en premier. Bouton hamburger repositionné dans la zone visible de l'écran. |
| 2026-06-10 22:30 | Site/feat | **Sidebar mobile transformée en drawer coulissant.** Overlay semi-transparent + animation `translateX`. Fermeture au clic overlay, touche Échap, ou lien sidebar. Focus piégé dans le drawer en mobile. |
| 2026-06-10 22:34 | a11y | **`:focus-visible` sur tous les éléments interactifs.** Anneau focus tokenisé (`border.focus`, teal.11) sur boutons, liens, toggles, inputs, sidebar. Bouton copier du code-block i18n (FR/EN). Clarification stat WCAG dans le site. |
| 2026-06-10 22:40 | style | **Cohérence globale : classes utilitaires, grille ADR, titres DOs/DON'Ts bilingues.** `.visually-hidden`, `.truncate`, etc. unifiés. Grille ADR reclassée. Titres sections DO/DON'T en FR et EN. |
| 2026-06-10 23:15 | fix(mobile) | **Sidebar-toggle dans le contenu avant `h1` + tables scrollables.** Bouton toggle sidebar injecté avant le titre principal (ordre DOM). Tables denses en `overflow-x:auto` sur mobile. |
| 2026-06-10 23:17 | Créé | `decisions/ADR-053-navigation-mobile.md`, `ADR-054-tables-responsives.md`, `ADR-055-focus-visible-i18n.md` · `decisions/README.md` mis à jour |
| 2026-06-11 00:34 | style(footer) | **Restructuration footer en 3 colonnes.** Col 1 : Guilherme Negreiros + LinkedIn. Col 2 : Changelog, Storybook, GitHub. Col 3 : copyright + crédit Claude Code (aligné à droite). Mobile : colonne unique, alignement gauche. |
| 2026-06-11 01:14 | style(footer) | **Footer finalisé** : Col 1 = logo Agentica + nom + LinkedIn (icône + texte). Col 2 = Changelog, Storybook, GitHub, Audit. Col 3 = copyright + « Développé avec Claude Code » (droite). |
| 2026-06-11 01:53 | fix(nav) | **Sidebar contextuelle — chaque section affiche uniquement ses sous-pages.** Fondations : couleur/espacement/typographie/icônes uniquement. Composants : pages composants uniquement. Décisions : ADRs uniquement (bloc Agents supprimé). Tokens : sidebar supprimée (page unique). |
| 2026-06-11 14:47 | fix(site) | **6 corrections diverses.** Table ADR : `white-space:nowrap` colonnes ADR+Date. Nav active : détection améliorée (sections array, top-level pages). Footer col 3 : `justify-self:end`. Carte Storybook dans Explorer. Stack logos officiels (CSS/Tailwind/Angular/Swift/Android). Changelog : timeline verticale, accordéon par version (dernière ouverte), sidebar ancres versions. |
| 2026-06-11 15:07 | chore | Rebuild `site/dist/audit.html` — horodatage mis à jour (build 15:06). |
| 2026-06-11 15:47 | fix(site) | **Changelog TOC droite + date gauche + home logos + footer col3.** Changelog : TOC versions colonne droite (script inline), date à gauche (grid 80px + 1fr), sidebar gauche supprimée. Home : carte Storybook et GitHub `target="_blank" rel="noopener"`. Illustration stack : logos officiels CSS/Tailwind/Angular/Swift/Android/React/Vue/Svelte (grille `platform-logos-grid`, remplace SVG `multi-platform`). Footer : col 3 `align-items:flex-start`. |
| 2026-06-11 17:14 | revert(home) | **Revert section Stack technique** — restauration SVG illustration + stack-flow d'origine (la grille `platform-logos-grid` remplaçait trop de contenu). |
| 2026-06-11 18:04 | fix(home) | **Remplacement des 6 disques colorés par logos officiels dans l'illustration Stack.** CSS, Angular, Swift, JavaScript, Tailwind CSS, Android injectés via `<image href="integrations/...">` aux coordonnées exactes des anciens cercles dans le SVG `multi-platform`. |
| 2026-06-11 18:16 | fix(changelog) | **Correction page Changelog vide.** Bug : collision de backtick dans le template littéral du `tocScript`. Fix : `JSON.stringify` des données avant injection. Fix bonus : `fill="none"` parasite supprimé du SVG CSS logo. |
| 2026-06-12 00:33 | chore(dist) | Rebuild `site/dist/audit.html`. |
| 2026-06-12 00:33 | fix(illustrations) | **Icônes de plateforme agrandies de 16×16 à 24×24** dans l'illustration SVG Stack pour corriger le rendu flou en production. |
| 2026-06-12 10:15 | Créé | `Prototype redesign.../inspiration/AI anti-patterns.md` + références visuelles : Paper design, Supernova, UX-pilot, The Design System Guide (images ajoutées dans `inspiration/références visuelles/`). |
| 2026-06-12 10:21 | chore(figma) | **Règles composants Figma + token `page-gutter` + ignore `Temp/`.** Création de `.claude/rules/figma-components.md` (règles de build plugin Figma : `vFill`, auto-layout, nommage, showcase instances, DO/DON'T, liens — §0 à §15). Ajout `space/layout/page-gutter` (80px) dans `primitives.json` + `semantic.json`. Mise à jour `dist/tokens/css/primitives.css`. `Temp/` ajouté à `.gitignore`. |
| 2026-06-12 10:23 | chore(figma) | Script plugin Phase 3 (`figma-phase3-plugin.js`) déplacé dans `scripts/figma/` — organisation des scripts plugin. |
| 2026-06-12 10:33 | feat(figma) | **Script plugin Segmented** (`scripts/figma/figma-segmented-plugin.js`) — ComponentSet 6 variantes (Tab=1/2/3 State=Default · Tab=1 State=Hover · Focus · Disabled) + page doc complète : `section-header` (gradient approche A + blobs), `section-showcase` (instances WRAP, dot grid), `section-states` (4 états), `section-tokens` (8 tokens liés), `section-dos-donts` (2 paires), `section-a11y`, `section-links`. Tous les fills via `vFill()`, police Atkinson Hyperlegible via `ahStyle()` (ADR-021), ComponentSet à `y=3000` + instances WRAP (règle §15). |
| 2026-06-12 10:57 | docs(figma) | **Règle §16 — frame « Composant principal » à `x=1600, y=0`.** Formalisation dans `.claude/rules/figma-components.md` : tout ComponentSet vit dans ce frame (auto-layout VERTICAL, padding 24px, gap 32px, fond `#FAFAFA`, bordure `#E8E8E8`). Chaque section = titre gras + liste des états + ComponentSet FIXED. Pattern `mkComposantPrincipal()` ajouté pour les builds futurs. Appliqué sur les 6 pages existantes (Button/Input/Toggle/Checkbox/Radio/Segmented). |
| 2026-06-12 15:02 | docs(figma) | **Cohérence police + tokens §16 dans `.claude/rules/figma-components.md`.** Migration de toutes les occurrences `Inter` → `Atkinson Hyperlegible` + `ahStyle()` (§5, §9, §10, §12 — 13 occurrences). §16 `mkComposantPrincipal()` : fills/strokes via `vFill()` / `setBoundVariableForPaint()` au lieu de `hex()` brut. |
| 2026-06-12 15:15 | docs(figma) | **Token `color/action/primary-subtle` ajouté au mapping §5.** Correction `descNode.fills` dans `mkHeaderSectionBold()` : RGB brut → `vFill("color/action/primary-subtle", "#F0FAF8")`. Token ajouté dans le tableau de mapping (teal.2, usage : texte description sur fond teal Approche B). |
| 2026-06-12 15:30 | fix(figma) | **Scripts plugin — suppression des RGB bruts et ajout de `vFill()`.** `figma-phase3-plugin.js` : ajout infra `VARS` + `vFill()` ; `styleSet()` fills → `vFill("color/background/hover")` ; knob/bar/dot fills → `vFill("color/text/on-action", "#FFFFFF")`. `figma-segmented-plugin.js` : `pg.backgrounds` déplacé après `hex()` + commentaire exception §0 (page.backgrounds ne supporte pas setBoundVariableForPaint). |
| 2026-06-12 15:45 | refactor(scripts) | **Robustesse `audit-tokens.js` et `axe-audit.js`.** `audit-tokens.js` : constante `TOKEN_PREFIXES` (3 préfixes centralisés), support `--src-dir` CLI, correction bug conversion CSS var → clé token (suppression préfixe `semantic.` erroné), comparaison de références en correspondance exacte (suppression `startsWith` trop permissif). `axe-audit.js` : support `--dist`, `--ci`, `--report-only`, `--exclude` en CLI ; rétrocompatibilité `AXE_BLOCKING` env var conservée. |
| 2026-06-12 16:00 | token(primitive) | **Ajout `primitive.space.0` = 2px** — demi-pas sub-grille (4px × 0.5) pour composants compacts. Correction `badge.sm.padding-y` : `"2px"` en dur → `{primitive.space.0}` (approbation explicite — modification token de composant). |
| 2026-06-12 20:50 | chore(rules) | **`figma-components.md` déplacé vers `instructions/`** — 1244 lignes hors auto-chargement. Stub 29 lignes conservé dans `rules/` pour la discoverabilité. |
| 2026-06-12 20:52 | chore(tokens) | **Regénération artefacts Style Dictionary** — `--agtc-primitive-space-0: 2px` présent dans tous les artefacts (CSS, JS, Tailwind, Swift, Android). |
| 2026-06-12 21:00 | feat(component) | **`agtc-tabs` créé — 6 surfaces** (Phase D3). Revue UX approuvée (patterns NN/g + W3C APG). Tokens `component.tabs.default` (8 tokens). Web Component Lit (shadow DOM) : tablist/tab/tabpanel ARIA, roving tabindex, activation auto, href optionnel, `:visited` neutralisé (ADR-047). Stories Storybook (4 stories). Guideline `guidelines/components/tabs.md`. ADR-056. Page site `tabs.html`. Build : 679 définies · 185 référencées · 0 fantôme · 74 fichiers. |
| 2026-06-12 16:15 | chore(tokens) | **Regénération des artefacts compilés via Style Dictionary.** `--agtc-primitive-space-0: 2px` désormais présent dans `dist/tokens/css/primitives.css`. Tous les formats mis à jour : CSS (primitives, semantic, components, all), JS, Tailwind, Angular SCSS, iOS Swift, Android XML. |
| 2026-06-12 16:27 | Créé | `.claude/rules/figma-components.md` |
| 2026-06-12 16:27 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-06-12 16:46 | Créé | `decisions/ADR-056-agtc-tabs-implementation.md` |
| 2026-06-12 19:13 | Créé | `decisions/ADR-057-deux-contextes-utilisation.md` |
| 2026-06-12 19:14 | Créé | `.claude/rules/contexts-utilisation.md` |
| 2026-06-12 19:14 | Modifié | `DESIGN.md` |
| 2026-06-12 19:30 | feat(foundations) | **Deux contextes d'utilisation — Phase complète.** Tokens primitifs : `fontSize.6xl` (60px), `space.24` (96px), `space.30` (120px). Tokens sémantiques : groupe `semantic.marketing.*` (7 tokens — typography.display, typography.eyebrow, space.section-breathing, space.hero-gap). ADR-057. Règle `.claude/rules/contexts-utilisation.md`. DESIGN.md section 7. Guideline `guidelines/foundations/contextes.md`. Site : `foundations/contextes.html`, CSS `[data-context="marketing"]`, `data-context="marketing"` sur home/get-started/agents. Build : 688 définies · 192 référencées · 0 fantôme · 75 fichiers. |
| 2026-06-12 20:04 | feat(site) | **Redesign Phase 1 — extension tokens CSS + dark mode + theme toggle + hero aurora.** `tokensCSS()` : bloc extension (space scale 1-10, `--agtc-header-height`, `--agtc-content-max`, shadows sm/md/lg/glow, gradients brand/text/aurora, palette accent rose + secondary prune, échelle typo detail→display, `color-scheme`) + bloc `:root[data-theme="dark"]` (surcharges sémantiques uniquement). `layout()` : `data-theme="light"` sur `<html>`, bouton `.theme-toggle` (soleil/lune) avant le lang-switch. `siteJS()` : toggle thème (prefers-color-scheme + localStorage `agtc-theme`). `siteCSS()` : header backdrop-filter + `var(--agtc-header-height)`, classes hero (aurora ::before, grille ::after, badge pulse, title gradient), `.layer-stack`/`.layer-plane` 3D, `.card-surface`/`.audience-card`/`.kpi-card`, `.stat-band`, `.ds-btn` (primary sans glow — anti-pattern), animations `auroraDrift`/`planeFloat`/`pulse`, reduced-motion. Offsets 60px → token. Pas de reveals scroll ni particules (décision anti-patterns). Build : 725 définies · 208 référencées · 0 fantôme · 75 fichiers. |
| 2026-06-12 20:08 | feat(site) | **Redesign Phase 2 — compteurs animés stat-band.** `siteJS()` : IntersectionObserver sur `.stat-band` uniquement (usage fonctionnel, pas décoratif) — anime les éléments `[data-count]` une seule fois (rAF, ease-out cubic, 1200ms, suffixe via `data-suffix`). `prefers-reduced-motion` : valeur affichée directement sans animation. Aucune particule ni reveal au scroll (décision anti-patterns). Build : 0 fantôme · 75 fichiers. |
| 2026-06-12 20:15 | feat(site) | **Redesign Phase 3 — hero home avec layer-stack 3D.** `buildHome()` : hero restructuré en grille 2 colonnes (`hero-grid`) — gauche : badge pulse + titre verbes (copy inchangé, contenu.md source de vérité) + tagline + actions `agtc-button` (dogfooding) ; droite : `layer-stack` 3 plans 3D (`planeFloat`) montrant la chaîne réelle primitive.color.teal.11 → color.action.primary → button.primary.background. Aurora en fond (`.hero::before`). Nettoyage CSS : suppression des doublons Phase 1 (`.ds-btn`, `.card-surface`, `.stat-band`, `.kpi-card`, `.audience-card` — les contrats card existants font foi), ancien bloc hero supprimé, media queries hero unifiées. Pas de `.grad` texte (1 gradient max/page = aurora). Build : 725 définies · 207 référencées · 0 fantôme · 75 fichiers. |
| 2026-06-12 20:24 | feat(site) | **Redesign Phase 4 — page couleur : recherche de tokens + tokens manquants.** `buildColor()` : champ `#token-search` + statut live (réutilise le filtre JS existant via wrapper `.token-section`), 3 tokens ajoutés au tableau sémantique (feedback.warning, background.hover, text.on-action — 19 au total, filtrés sur existence dans SEM), compteur dynamique dans l'intro, valeur `#0d74ce` en dur dans la blockquote → `SEM['color-action-primary']`. Gouvernance : suppression des overrides light de `background-inverse`/`text-on-inverse(-muted)`/`feedback-warning`/`success-subtle` du bloc extension (les valeurs de tokens/semantic.json font foi — pas de contournement CSS). Build : 0 fantôme · 75 fichiers. |
| 2026-06-12 20:32 | chore(site) | **Redesign Phase 5 — vérification : corrections UX déjà couvertes.** Audit du fichier `Redesign/corrections/corrections-site-10-06-2026.md` contre l'état du site : grille métadonnées ADR (`.adr-meta` grid, sans duplication), `token-table th` sans retour à la ligne, logos officiels (`integrations/`), i18n bouton Copier (ADR-041), version v0.1.0 uniforme (0 résidu v1.0.0), footer aligné — tout était déjà en place (sessions 10-11 juin). Vérif end-to-end : aurora limitée à la home (1 gradient/page), `.hero` absent des pages produit, 0 fantôme CSS, CI vert phases 1-4. Redesign complet. |
| 2026-06-12 21:26 | Créé | `decisions/ADR-058-redesign-site-theme-sombre.md` |
| 2026-06-12 21:26 | Modifié | `decisions/README.md` |
| 2026-06-12 20:40 | Créé | `decisions/ADR-058-redesign-site-theme-sombre.md` — décision du redesign : thème sombre (`data-theme`, surcharges sémantiques uniquement), tokens d'extension CSS (règle : jamais de redéfinition d'un token JSON en light), arbitrage animations vs anti-patterns (aurora/planeFloat/compteurs ✅ · reveals/particules/glow ❌). Index `decisions/README.md` rattrapé : ADR-053 → 058 ajoutés (053-057 manquaient). Build : 76 fichiers. |
| 2026-06-13 07:30 | fix(site)+a11y | **Audit structurel — 6 catégories corrigées.** (1) Performance : `<link rel="preconnect">` Google Fonts ajouté dans `<head>`. (2) CSS tokenisé : `--agtc-site-header-padding-x`, `--agtc-site-sidebar-width`, `--agtc-site-toc-width` → remplacent les `px` en dur sur header/sidebar/TOC. (3) Icônes nav-card : CSS base `width/height:32px` → `var(--agtc-semantic-icon-size-nav)` (24px) ; marketing override (32px) conservé ; JS calls SaaS `32→24`. (4) Paddings off-grille : `principle-card{padding:22px}` + `kpi-card{padding:20px 22px}` → `var(--agtc-component-card-default-padding)` (24px). (5) Stack technique : `icon(...,18)` → 20px ; `width/height="22"` → 24px sur Lit SVG, Storybook img, React img. (6) ADR header redesigné : suppression doublons (H1+blockquote frontmatter éliminés du parseMd), nouveau `.adr-header` (pill ADR-NNN + badge Actif + type + H1 propre + dl métadonnées Date/Décideurs). `loadADRs()` extrait aussi `type`. Build : 757 définies · 244 référencées · 0 fantôme · 76 fichiers. |
| 2026-06-13 06:00 | style(site) | **Padding : meilleure pratique section vs conteneur.** Pattern "full-bleed section + inner container" appliqué. `.content` (conteneur `<main>` des pages doc) : `padding:52px 0` → `padding:52px 64px` (restauration `padding-inline`). `.kpi-card` ajouté au bloc `[data-context="marketing"]` comfortable padding (alignement avec `.audience-card`). Responsive : `.content{padding:28px 20px}` et `.home-section{padding:40px 20px}` (breathing room sur mobile). Règle : sections → `padding-block` seulement · conteneurs → `padding-inline`. Build : 0 fantôme · 76 fichiers. |
| 2026-06-15 — | fix(site) | **stack-node — token font-size cohérent.** `--agtc-semantic-typography-label-size` → `--agtc-component-card-typography-title-size` sur `.stack-node-title` et `.stack-node-sub`. Même famille que `card-typography-title-weight` déjà en place. Font-size explicite sur chaque enfant ; héritage via container retiré. Build : 0 fantôme · 755 définies. |
| 2026-06-13 07:45 | refactor(site) | **Harmonisation sélecteurs d'icônes — :is(svg,img).** Suppression des règles dupliquées `.tool-card-icon .vendor-logo` + `.tool-card-icon svg` (même taille, sélecteurs différents). Remplacées par `.tool-card-icon :is(svg,img)` (un seul sélecteur). Idem pour `.stack-node-icon svg` → `.stack-node-icon :is(svg,img)`. La classe `.vendor-logo` reste pour son usage dans les tableaux et cellules platform (display/object-fit). Build : 0 fantôme · 755 définies. |
| 2026-06-13 07:30 | refactor(site) | **Tokens fontWeight — zéro usage primitif en CSS site.** 40+ occurrences de `--agtc-primitive-fontWeight-{regular|semibold|bold|extrabold}` remplacées par tokens sémantiques via sed. Mapping : `regular→body-weight(400)`, `semibold→label-weight(500)`, `bold→semantic-fontWeight-bold(700, nouveau)`, `extrabold→semantic-fontWeight-display(800, nouveau)`. Extension `:root` : suppression des 4 définitions primitives orphelines + ajout 2 tokens sémantiques. COMP : `card-typography-title-weight` et variantes marketing migrent vers `semantic-fontWeight-bold`. Build : 0 fantôme · 755 définies · 242 référencées. |
| 2026-06-13 07:15 | fix(site) | **stack-node — sémantique classe + token.** `.stack-node-label` → `.stack-node-title` (c'est un titre de nœud, pas un label de formulaire). `--agtc-primitive-fontWeight-bold` → `--agtc-component-card-typography-title-weight` (couche composant, 700 — interdit d'utiliser un primitif directement en CSS site). Build : 0 fantôme. |
| 2026-06-13 07:00 | fix(site) | **Section "Stack technique" — taille de texte unique.** Règle : une seule `font-size` par section (hiérarchie via `font-weight` + `color`). `stack-node` : `font-size:label-size(14px)` déplacé sur le conteneur parent ; `stack-node-label` et `stack-node-sub` héritent (suppression des déclarations explicites). `tool-card-role` : `font-size:meta-size(12px)` supprimé — le `<span>` hérite `label-size` de `.tool-card-name`. `tool-card-desc` : `meta-size(12px)` → `title-size(14px)` explicite. Build : 0 fantôme · 757 définies · 244 référencées. |
| 2026-06-13 06:30 | fix(site) | **Section "Stack technique" — cohérence icônes tool-card + token stack-node-icon.** Retrait de `.tool-card-icon .vendor-logo` du groupe marketing `feature-size` (32px) — les vendor logos (Storybook, React) restent à `icon-size-nav` (24px) = cohérent avec les icônes SVG Lucide (Style Dictionary, axe-core, Playwright). Les tool-cards sont des éléments compacts : 32px hors contexte. Ajout `.stack-node-icon svg{width/height:icon-size-control(20px)}` — taille tokenisée (était HTML attr hardcodé). Build : 0 fantôme · 757 définies · 244 référencées. |
| 2026-06-13 06:00 | fix(site) | **Section "Stack technique" — grille 4px + hiérarchie typographique.** `stack-node{padding:18px 14px}` → `16px 12px` (grille 4px). `.stack-node-label{font-size:detail-size}` → `label-size` (14px) — hiérarchie visuelle label > sous-titre. `.stack-node-sub{margin-top:3px}` → `4px`. `.tool-card-icon{margin-top:2px}` → `0`. `.tool-card-desc{margin-top:3px}` → `4px`. Ajout `.tool-card-icon svg{width/height:icon-size-nav}` — Lucide icons alignés à 24px comme vendor logos (cohérence). Retrait `.tool-card-name` de l'override marketing heading-5 (20px) — outil compact horizontal, reste à 14px même en marketing. Build : 0 fantôme · 757 définies · 244 référencées. |
| 2026-06-13 05:00 | token(icon)+style(site) | **Padding sections + icônes grille 4px.** Sections : `.content{padding:52px 0}` et `.home-section{padding:64px 0}` (idem responsive 28/40px) — suppression des 64px/72px/20px horizontaux sur les deux contextes. Tokens : `primitive.iconSize.xl:32px` + `semantic.icon.size.feature:32px`. Icônes 22px → 24px (`icon-size-nav`) en base (menu, github, audience ×4) ; `.tool-card-icon .vendor-logo` → `var(--agtc-semantic-icon-size-nav)`. Override `[data-context="marketing"]` : `.audience-icon svg/.info-card-icon svg/.nav-card-icon svg/.tool-card-icon .vendor-logo` → `var(--agtc-semantic-icon-size-feature)` (32px). Build : 0 fantôme · 754 définies · 241 référencées. |
| 2026-06-13 04:30 | token(card) | **Corrections typographie + espacement marketing.** `card.typography.marketing.title.size` : `body.size` (16px) → `heading.5.size` (20px) dans `tokens/component.json` et COMP. Espacement confortable : 5 tokens `--agtc-semantic-space-comfortable-*` ajoutés dans l'extension `:root` (valeurs pré-résolues — `ceil()` invalide en CSS natif) : `layout-component:28px`, `layout-section:40px`, `control-padding-x:20px`, `control-padding-y:12px`, `control-gap:12px`. Bloc `[data-context="marketing"]` : 6 sélecteurs `.principle-card/.step-card/.nav-card/.info-card/.tool-card/.audience-card` → `padding:var(--agtc-semantic-space-comfortable-layout-component)`. Build : 0 fantôme · 753 définies · 239 référencées. |
| 2026-06-15 — | fix(site)/audit | **Audit site complet — 4 violations immédiates corrigées.** (1) `:visited` fallbacks hex/rgba obsolètes (pattern IE11) retirés des 4 règles de navigation (top-nav, footer-links, audit-footer-link, storybook-btn) — token seul conservé (règle `no-visited-nav`). (2) Hex fallbacks `#fff`/`#1a1820`/`#646464` retirés des appels `var(,fallback)` dans les overrides `[data-theme="light"]` — confiance aux tokens. (3) `audit-contrast-table{font-size:0.85rem}` → `var(--agtc-semantic-typography-label-size)`. (4) `filter:drop-shadow(rgba...)` tokenisé via nouveau `--agtc-drop-shadow-sm` dans l'extension `:root`. Build : 0 fantôme. |
| 2026-06-15 — | token(semantic) | **18 tokens sémantiques créés — fermeture de la hiérarchie 3 niveaux.** Avant : ~20 tokens composant (badge, code-block, banner, toggle, card-padding, table) référençaient des primitives directement. Nouveaux tokens dans `semantic.json` : `color.brand.primary-text` (teal.12) ; `color.feedback.info-text` (blue.12), `feedback.warning-text` (orange.12) ; `color.background.code`/`code-raised`/`code-raised-hover` (gray.12/11/10) ; `color.text.on-code`/`on-code-muted`/`on-code-strong` (gray.4/8/1) ; `color.control.track-off`/`track-off-hover` (gray.9/10) ; `space.component.padding-2xs`→`padding-2xl` (2px/4px/8px/12px/16px/20px/24px). COMP mis à jour : 0 primitive référencée directement. Build : 774 définies · 242 référencées · 0 fantôme · 76 fichiers. |
| 2026-06-13 04:00 | token(card) | **Typographie card — dual contexte SaaS / Marketing.** Ajout de `card.typography.*` dans `tokens/component.json` : 3 niveaux SaaS (title/body/meta → 14/14/12px) + 4 tokens marketing (title→16px, hero-title→20px, body→16px, meta→14px). COMP dans `site/build.js` étendu de 11 tokens `card-typography-*`. `siteCSS()` : toutes les classes `.nav-card-title`, `.principle-title`, `.info-card-title`, `.step-card-title`, `.tool-card-name`, `.audience-title`, `.nav-card-desc`, `.principle-desc`, `.info-card-body`, `.audience-desc` migrent de `semantic-typography-label-size` → `component-card-typography-*`. Bloc `[data-context="marketing"]` ajouté (10 règles d'override). Bonus : `.kpi-num font-size:2rem` → `heading-2-size`. `agtc-card.js` : `:host` expose `--card-title/body/meta-size` + `::slotted(h*)` et `::slotted(p)`. `guidelines/components/card.md` : section TOKENS — dual contexte documentée. Build : 0 fantôme · 753 définies · 238 référencées (+10). |
| 2026-06-13 03:15 | refactor(site) | **Audit conformité tokens — Phase 3 : 8 violations résiduelles.** Ajout `--agtc-surface-overlay:rgba(0,0,0,.40)`. Corrections : `font-weight:400` → `fontWeight-regular` · `letter-spacing:-.01em` → `tracking-heading` · `border-radius:9999px` (×4) → `radius-pill` · `border-radius:6px` (×2 swatches) → `radius-control` · sidebar overlay → `surface-overlay`. Note : `letter-spacing:.12em` en ligne 4537 = contenu `<pre>`, non modifié. Build : 0 fantôme · 742 définies · 228 référencées. |
| 2026-06-12 23:30 | refactor(site) | **Audit conformité tokens — Phase 2 : font-weight, letter-spacing, tailles hors-échelle.** Ajout de 14 tokens d'extension dans `:root` : 4 `--agtc-primitive-fontWeight-*` (regular/semibold/bold/extrabold) + 10 `--agtc-tracking-*` (tighter/tight/snug/heading/wide/wider/label/loose/overline/eyebrow). Remplacement de 56 `font-weight:[0-9]+` → tokens primitifs, 28 `letter-spacing:.*em` → tokens tracking, 8 font-sizes hors-échelle Minor Third (0.8–0.825–0.8125rem → detail-size · 0.95rem → body-size · 1.1rem → heading-5-size). `.logo-name` à `1.05rem` conservée (absence de token intermédiaire — dette documentée). Build : 0 fantôme CSS · 741 définies · 226 référencées. |
| 2026-06-12 22:30 | feat(site) | **Marketing context complet — page d'accueil (ADR-057).** `siteCSS()` : 3 règles `[data-context="marketing"]` — `h2` → `heading.1` (2.5rem/40px) via `--agtc-semantic-typography-heading-1-size`, intro `p` → `body.size` (1rem), pas de conflit couleur avec sections `.home-section-ink` (cascade préservée). `buildHome()` : `.marketing-section` ajoutée aux 8 sections → active `section-breathing` (96px, token `--agtc-semantic-marketing-space-section-breathing`). Suppression `style="padding-top:0"` (valeur brute interdite). SEO : 1 seul H1 (hero), 8 H2 de section intacts. Build : 0 fantôme CSS, 208 références (+2). |
| 2026-06-12 22:00 | fix(site) | **Dark mode toggle — bug SyntaxError `const statBand` dupliqué.** La déclaration `const statBand` apparaissait deux fois dans le même scope `DOMContentLoaded` de `siteJS()` (lignes 1186 et 1311) — provoquait `SyntaxError: Identifier 'statBand' has already been declared`, bloquant tous les event listeners (dont le theme toggle). Suppression de l'ancien bloc (sans `prefers-reduced-motion`). Vérifié en production via Playwright : `light → dark` ✅, `bg: rgb(10,12,17)` ✅, `action-primary: #34d3bb` ✅, 0 erreur JS. |
| 2026-06-15 09:27 | Créé | `decisions/ADR-059-semantic-tokens-hierarchy-completion.md` |
| 2026-06-15 09:27 | Modifié | `decisions/README.md` |
| 2026-06-15 — | fix(site)+docs | **Correction `:visited` navigation — bug Safari persistant (3ème rapport).** Erreur de session : les valeurs hex littérales dans les règles `:visited` avaient été supprimées lors de l'audit du jour, qualifiées à tort de "fallbacks IE11 obsolètes". Il s'agissait en réalité d'une nécessité de compatibilité **Safari** : WebKit bloque la résolution de `var()` dans `:visited` pour prévenir le history sniffing. Pattern restauré : `color:#646464;color:var(--agtc-semantic-color-text-secondary)`. Ajout du bloc dark mode `:visited` (`[data-theme="dark"]`) avec valeurs résolues sombres (`#a4abb8`, `#04201c`). Documentation mise à jour : `no-visited-nav.md` §Exception Safari + ADR-059 note post-incident. Build : 774 · 0 fantôme. |
| 2026-06-15 — | a11y(site) | **Focus visible navigation + `aria-current` + actif mobile.** (1) `:focus-visible` absent sur `.top-nav a`, `.github-btn`, `.storybook-btn`, `.footer-links a` → `outline:2px solid border-focus; outline-offset:2px`. (2) `.sidebar a:focus-visible` utilisait `outline:none` seul → remplacé par `box-shadow:inset 0 0 0 2px border-focus` (ring interne adapté à la pleine largeur). (3) `.nav-card:focus-visible` séparé de `:hover` : plus de `transform:translateY` au focus clavier (instabilité spatiale), `outline:2px` à la place. (4) `aria-current="page"` absent sur les liens actifs — lecteurs d'écran ne voyaient que la classe `.active` (WCAG 4.1.2) — ajouté en JS sur top-nav et sidebar. (5) Mobile : `.top-nav a.active` utilisait `border-bottom` sur nav verticale → `border-left:3px + padding-left` compensé. Build : 774 · 0 fantôme. |
| 2026-06-15 — | style(site) | **`.top-nav a.active` — indicateur de ligne, pas de fond.** `.active` utilisait `background:subtle` → ressemblait à un bouton togglé/pill. Correction : `background:transparent` + `border-bottom:2px solid action-primary` + `font-weight:bold`. Pattern standard nav horizontale (NN/g) : la page courante s'indique par une ligne, pas un fond rempli. Build : 774 · 0 fantôme. |
| 2026-06-15 — | a11y(site) | **États `:active` navigation — comportement lien, pas bouton.** Aucune règle `:active` n'existait sur les éléments de nav → le navigateur affichait sa couleur par défaut (rouge/violet). Ajout sur 8 zones : `top-nav a`, `nav-cta`, `github-btn`, `storybook-btn`, `sidebar a`, `nav-card`, `toc a`, `footer-links a`, `audit-footer-link`. Convention : `:active` maintient le style `:hover` sans effet de pression (pas de `transform`, pas de `box-shadow` supplémentaire). Exception `nav-card:active` : `transform:none` — le `translateY(-1px)` du hover est annulé, la carte ne s'enfonce pas comme un bouton. Build : 774 · 0 fantôme. |
| 2026-06-15 — | feat(ds)+refactor(site) | **Composant `agtc-top-nav` — formalisation complète (6 surfaces).** Déclencheur : 6 corrections nav en une journée → site = laboratoire du DS, formalisation obligatoire. (1) **Tokens** : 18 nouveaux dans `tokens/component.json` (groupe `top-nav.*` : 11 tabs + 7 CTA). (2) **COMP object** : 18 entrées `top-nav-*` → CSS du site référence `--agtc-component-top-nav-*`. (3) **Guideline** : `guidelines/components/top-nav.md`. (4) **Web Component** : `components/agtc-top-nav.js` — Lit, shadow DOM, `_isActive()`, `:visited` neutralisé. (5) **Storybook** : `components/agtc-top-nav.stories.js` — 4 stories. (6) **ADR-060** + `decisions/README.md`. Distinction clé : `role=navigation` + `aria-current="page"` (pas `role=tablist`). Build : 792 définies +18 · 0 fantôme. |
| 2026-06-15 11:29 | Créé | `decisions/ADR-061-agtc-top-nav-v1-1-bilinguisme-mobile-integration.md` |
| 2026-06-15 11:29 | Modifié | `decisions/README.md` |
| 2026-06-15 — | feat(ds)+feat(site) | **`agtc-top-nav` v1.1 — bilinguisme, mobile shadow DOM, intégration site (ADR-061).** (1) **Bilinguisme** : `agtc-top-nav.js` étend la structure d'item (`labelFr`/`labelEn`) et observe `document.documentElement[data-lang]` via `MutationObserver` pour re-render automatiquement à chaque changement de langue. (2) **Mobile shadow DOM** : le CSS mobile (drawer vertical, `:host(.open) nav`, `a` responsive) est déplacé entièrement dans le shadow DOM du composant — `site.css` ne contient plus aucun `.top-nav*`. (3) **Bundling esbuild** : `bundleComponents()` dans `site/build.js` (CJS) appelle esbuild via `execSync` → bundle IIFE auto-contenu `site/dist/components/agtc-top-nav.js` (~21kb). (4) **Intégration site** : le nav HTML statique `<nav class="top-nav">` est remplacé par `<agtc-top-nav id="site-top-nav">` + script d'init différé (`customElements.whenDefined`). `aria-controls` du hamburger mis à jour. JS actif top-nav retiré (composant le gère). Build : 792 · 242 · 0 fantôme. |
| 2026-06-15 12:00 | Créé | `decisions/ADR-062-bundle-unique-wc-dogfooding-phase1.md` |
| 2026-06-22 — | perf(site): optimisations P1 — SVG lazy-load, Google Fonts non-bloquant | **Home page : 1 453 KB → 65 KB (réduction 95 %).** (1) `site/build.js` : les 3 illustrations SVG (`human-last-word.svg`, `pipeline-tokens.svg`, `multi-platform.svg`) ne sont plus inlinées dans le HTML — remplacées par `<div class="illus-lazy" data-svg="img/...">` et copiées dans `site/dist/img/`. (2) `siteJS()` : IntersectionObserver charge et injecte le SVG inline à l'approche de la section (rootMargin 400px) — CSS custom properties (dark mode) conservées. (3) Google Fonts : `@import url(...)` supprimé de `site.css` → `<link rel="stylesheet">` dans le `<head>` HTML — élimine 2 allers-retours réseau en cascade. (4) CSS : règle `.illus-lazy[data-svg]` avec `aspect-ratio: 800/420` pour éviter le layout shift (CLS = 0). Standards documentés dans `.claude/rules/performance.md`. Build : 796 · 262 · 0 fantôme · 95 fichiers. |
| 2026-06-20 — | fix(nav)+fix(site)+feat(seo) | **Nav Pipelines toujours actif corrigé + badge version supprimé + robots.txt/sitemap.xml.** (1) `components/agtc-top-nav.js` : `'pipelines'` ajouté à `SECTIONS` — `_isActive()` utilisait `endsWith('/index.html')` au lieu de `includes('/pipelines/')`. (2) `site/build.js` : `<span class="logo-version">v0.1.0</span>` retiré du header, CSS `.logo-version` et règle mobile nettoyés. (3) `buildRobotsAndSitemap(adrs)` : génère `robots.txt` (Disallow: /audit.html, pointeur sitemap) et `sitemap.xml` (104 URLs, priorités par section, lastmod auto). Constante `SITE_URL` ajoutée. Build : 796 · 262 · 0 fantôme · 95 fichiers. |
| 2026-06-20 — | feat(site)+feat(pipelines) | **Section Pipelines & Workflows — 15 nouvelles pages + promo home.** `site/build.js` : (1) Constante `PIPELINES` (14 entrées : 1 orchestrateur `quality-gate`, 10 actifs bloquants, 3 planifiés) avec contenu bilingue complet — titre, desc, objectif, marketing copy, 3 étapes visuelles, contrôles numérotés, matrice déclenchement, commande. (2) `buildPipelinesIndex()` : page d'index ton marketing — stat-band (10/3/14), cards nav-grid par catégorie. (3) `buildPipelinePage(p)` : page de détail par pipeline — badges statut/bloquant, illustration `.pipeline` 3 étapes numérotées, valeur métier, matrice déclenchement, liste numérotée contrôles, bloc commande. (4) `sidebarPipelines(base, current)` : navigation latérale avec liens actifs. (5) Navitem "Pipelines" ajouté entre Agents et Démarrer. (6) Card "Pipelines qualité" dans `sections` nav-grid de la home. (7) Section promotionnelle `.section-secondary` sur la home (4 mini-cards : wcag, tokens-audit, chromatic, commit + CTA). (8) Image sociale : `Agentica social image.jpg` → `agentica-social-image.png`, URL meta og:image/twitter:image mise à jour. Build : 796 · 262 · 0 fantôme · 95 fichiers. |
| 2026-06-15 12:00 | Modifié | `decisions/README.md` |
| 2026-06-15 — | feat(ds)+feat(site) | **Bundle unique `agtc.js` + dogfooding WC Phase 1 (ADR-062).** (1) **`components/index.js`** : point d'entrée unique important les 16 composants Agentica en ordre de dépendance (`agtc-icon` en premier). `bundleComponents()` simplifié : 1 bundle `site/dist/components/agtc.js` (~526kb Lit + tous WC) au lieu de N fichiers. `<head>` : un seul `<script defer src="components/agtc.js">`. (2) **Dogfooding doc pages** : les simulations CSS remplacées par les vrais WC — `button.html` (12 × `<button class="agtc-button">` → `<agtc-button>`), `banner.html` (`<div class="agtc-banner">` → `<agtc-banner>`), `badge.html` (inline style→ `<agtc-badge>`), `link.html` (`<a class="agtc-link">` → `<agtc-link>`), `segmented.html` (`<div class="agtc-segmented">` → `<agtc-segmented>` avec options JSON). (3) **Site UI** : `contributionBanner()` → `<agtc-banner variant="brand" icon="github">` + `<agtc-link slot="actions" external>`, 2 banners `get-started.html` → `<agtc-banner variant="info/brand">`, 61 × `statusBadge` `decisions/` → `<agtc-badge variant="success" size="sm" icon="circle-check">`. Exceptions maintenues : `<a class="agtc-button">` nav (liens sémantiques), `lang-switch` (JS couplé). Build : 792 · 236 · 0 fantôme · 79 fichiers. |
| 2026-06-19 — | token(semantic) | **Enrichissement DTCG agent-ready — 10 tokens prioritaires annotés (ADR-052).** Ajout de `$description`, `$deprecated:false` et `$extensions['com.agentica.usage']` sur les tokens sémantiques les plus à risque de mauvaise interprétation IA : `color.action.primary/hover/disabled`, `color.feedback.danger/success/info`, `color.text.primary/secondary`, `color.background.page`, `color.border.focus`. Chaque token documente son `role`, ses `use[]` autorisés, un `doNotUse` explicite (pièges nommés : PIÈGE DES DEUX ROUGES danger≠brand.accent, action.primary≠brand.primary, bleu info≠bleu interactif, border.focus sans :focus-visible) et le `decision` ADR. Idem pour `button.primary` et `button.critical` dans `component.json` avec cross-references `doNotUse`. Convention `_extensions-convention` ajoutée dans `$metadata` des deux fichiers. `$value` inchangées — annotation pure, ignorée par Style Dictionary. Commit 34471f7. |
| 2026-06-19 — | fix(tokens)+fix(site) | **Correction référence cassée `semantic.fontWeight.bold` → `primitive.fontWeight.bold`.** `tokens/component.json` · `top-nav.tab.font-weight-active` référençait `{semantic.fontWeight.bold}` inexistant — bloquait Style Dictionary build depuis l'introduction de `agtc-top-nav` (ADR-060). Corrigé en `{primitive.fontWeight.bold}`, cohérent avec les 30+ autres références bold du système. Build passe désormais sans erreur (les warnings `filtered out token references` sont pré-existants — combinaison filtre + `outputReferences`, comportement attendu). Rebuild site : 796 définies · 261 référencées · 0 fantôme · 80 fichiers. Commit 3b080b5. |
| 2026-06-16 — | fix+feat(site) | **Session redesign — corrections accessibilité, brand, icônes, changelog, badge.** (1) **`:focus-visible` systématique** (ADR-055) : étendu à `.sidebar a`, `.nav-card`, `.exp-tab`, `.toc a`, `.menu-toggle`, `.sidebar-toggle`, `.explorer-search`. (2) **i18n bouton Copier** : texte calculé depuis `data-lang` à l'init et au changement de langue — "Copier"/"Copy", "Copié !"/"Copied!". (3) **Tables responsives** (ADR-054) : post-processing `layout()` enveloppe toutes les `<table>` dans `.table-wrap { overflow-x:auto }` avec `min-width:420px`. (4) **Sidebar contextuelle** : chaque section n'affiche que ses sous-pages (Fondations, Composants, Décisions). (5) **Footer 3 colonnes** : logo+LinkedIn / liens navigation / copyright+crédit — col3 alignée à gauche. (6) **Timeline Changelog** : grille `80px 1fr`, accordéons `<details>`, TOC ancres en colonne droite, injection via `JSON.stringify` (correction bug backtick). (7) **Icônes plateforme SVG** : remplacement des 6 disques colorés par `<image href="integrations/*.svg">` dans `multi-platform.svg`, taille 24×24px. Logos officiels copiés depuis `Redesign/icon logos/`. (8) **`agtc-badge` conformité** : `agtc-badge.js` mis à jour pour utiliser `--agtc-component-badge-*` (convention `code-style.md`) — suppression des aliases de contournement. (9) **Couleurs de marque introduites** : `color.brand.accent` (rose-corail) sur stats et KPI-num ; `color.brand.secondary-text` (bordeaux) sur titres de cards et `.adr-type` ; `.changelog-item-date` et point timeline en accent. (10) **Page Couleurs** : nouvelle section "Couleurs de marque" avec les 11 tokens `color.brand.*`. (11) **Carte KPI "3 niveaux" supprimée**. Build : 796 · 264 · 0 fantôme · 80 fichiers. |
| 2026-06-22 12:06 | Créé | `.claude/rules/performance.md` |
| 2026-06-25 09:02 | Créé | `decisions/ADR-063-agtc-feature-card.md` |
| 2026-06-25 — | feat(redesign)+feat(component) | **9 corrections V2 + composant `agtc-feature-card`.** (1) **Footer 4 colonnes V2** : Brand · Navigation · Documentation · Explorer — radial gradient teal, `v2-footer-grid`. (2) **Dark mode global** : `data-theme="dark"` sur toutes les pages (non plus conditionnel). (3) **Teal-9 en dark mode** : `--agtc-semantic-color-action-primary:#12a594` via override `[data-theme="dark"]` — action tokens + button tokens. (4) **Cards rôles uniformes** : `align-items:stretch` + `flex-direction:column` sur `.v2-role-grid/.v2-role-card`. (5) **Logo 3 couleurs** (rose #FF8AA1 / gris #6B7280 / teal #12A594) dans header et footer — 3 `<path>` distincts depuis `Brand/Logo Agentica - color white.svg`. (6) **Sidebar gauche V2** : composant `v2Sidebar()` sticky 220px sur les 5 sous-pages marketing (pourquoi, architecture, qualité, ia, documentation) — `aria-current="page"` dynamique, responsive (hidden < 860px). (7) **Icônes blocs éditoriaux** : `v2-editorial-icon` (carré teal 2.5rem, fond rgba teal 12%) sur chaque bloc des 4 sous-pages — book-open, bot, users, shield-check (Pourquoi) · layers, code, settings, git-branch (Architecture) · shield-check, eye, layers, file-text (Qualité) · zap, lock, brain, network (IA). (8) **Border toggle thème supprimé** : `border:none !important; background:transparent !important`. (9) **Border-bottom cards** : `var(--agtc-semantic-color-action-primary)` (SaaS) · gradient primary→accent sur `body[data-context="marketing"]`. (10) **`agtc-feature-card`** : nouveau Web Component Lit — `heading`, `heading-level` (1-6, défaut 3), `variant` (default/marketing), slot `icon` + slot défaut — `prefers-reduced-motion` dans shadow CSS (border visible à pleine largeur) — `role="heading"` + `aria-level` dynamique. Revue UX P1-P8 approuvée. Guideline, ADR-063, Storybook stories créés. Build : 804 définies · 269 référencées · 0 fantôme · 96 fichiers. |
| 2026-06-25 10:22 | Créé | `decisions/ADR-064-v2-light-mode-implementation.md` |
| 2026-06-25 — | fix(theme)+audit | **Mode clair V2 + audits WCAG/UX.** (1) **Découplage dark/light V2** : suppression de `color-scheme:dark` hardcodé sur `body.v2-page` ; remplacement de `background-inverse` → `background-page` (adaptatif) sur body, header, docs-panel, footer, mobile-nav. (2) **Bloc `[data-theme="light"]`** ajouté dans `siteCSS()` : réassignation de toutes les variables `--v2-*` vers les tokens sémantiques light (`text-primary`, `text-secondary`, `border-default`, `background-subtle/surface`) ; surcharges composants : role-card (`background-surface` + `shadow-card`), signal-stack (`background-subtle`), docs-panel (`background-surface` + `shadow-raised`), button-secondary/ghost, kicker (`action-primary`), quality overlay. Aurora light : 2 radiales (teal 7%, rose 6%) — pas de brun. (3) **Audit WCAG 2.2** : 0 violation · 1541 vérifications · 111 pages — ratio minimum mesuré : 4.65:1 (`action-primary`/`background-page`). `scroll-padding-top:calc(var(--agtc-header-height,64px) + 12px)` déjà en place (SC 2.4.11). (4) **Audit UX patterns** : non déclenché — aucun composant modifié, toggle de thème pre-existant, changement classé « correction d'implémentation ». (5) **ADR-064** créé — documente la décision + amende ADR-058 sur le défaut dark des pages. (6) **decisions/README.md** mis à jour (ADR-063 et ADR-064 ajoutés à l'index). Build : 0 fantôme CSS. |
| 2026-06-25 10:23 | Modifié | `decisions/README.md` |
| 2026-06-26 15:05 | Créé | `.claude/rules/illustrations-source.md` |
| 2026-06-29 09:00 | Ajouté | `Brand/illustrations/IMG-FUTURE.png` — restauré (slide 16 présentations, non référencé sur le site) ; `illustrations-source.md` : tableau "Fichiers présentation uniquement" + règle anti-suppression sans vérification |
| 2026-06-29 09:10 | Modifié | `.claude/rules/illustrations-source.md` |
| 2026-06-29 09:11 | Modifié | `.claude/rules/illustrations-source.md` |
| 2026-06-29 09:11 | Modifié | `.claude/rules/illustrations-source.md` |
| 2026-06-29 13:20 | Renommé | `on-inverse` → `on-dark` — 3 tokens dans `tokens/semantic.json` (`text.on-inverse*` → `text.on-dark*`, `border.on-inverse` → `border.on-dark`) + 50+ occurrences dans `site/build.js` + `components/agtc-feature-card.js`. Convention EightShapes (plus directe que Material Design `on-inverse`). Voir ADR-065. |
| 2026-06-29 13:25 | fix(a11y) | `components/agtc-feature-card.js` — contraste Chromatic 1.12 corrigé. Cause : `.heading`/`.body` utilisaient `text.primary` (#202020) sur fond glassmorphism toujours sombre → 1.12:1. Fix : `text.on-dark` (rgba blanc) pour `.heading`, `text.on-dark-secondary` pour `.body`. Fond dark-only → token `on-dark` (pas `text.primary`). 10 violations Chromatic → 0. |
| 2026-06-29 13:30 | Créé | `tokens/semantic.dark.json` — 38 tokens delta dark mode (DTCG-conforme, `$value/$type/$intent`). Pattern "deux fichiers, un seul build CSS" (Primer/GitHub). Contient UNIQUEMENT les valeurs qui changent vs mode clair : background (5), text (8), action (3), feedback (11), brand (7), border (3), illustration (1). |
| 2026-06-29 13:35 | Modifié | `style-dictionary/build.cjs` — format `css/dark-mode` enregistré (wrapper `[data-theme="dark"] { ... color-scheme: dark; }`) + second run `sdDark` (source : `tokens/semantic.dark.json` uniquement → `dist/tokens/css/dark.css`). `npm run tokens` produit désormais 5 fichiers CSS : primitives / semantic / components / all / dark. |
| 2026-06-29 13:35 | Généré | `dist/tokens/css/dark.css` — 38 variables CSS sous `[data-theme="dark"]`. Chargé après `all.css` dans Storybook et les consommateurs. |
| 2026-06-29 13:40 | Installé | `@storybook/addon-themes` (devDependency) |
| 2026-06-29 13:40 | Créé | `.storybook/modes.js` — `allModes` {light: `#fcfcfc`, dark: `#0a0c11`} pour Chromatic Story Modes |
| 2026-06-29 13:40 | Modifié | `.storybook/main.js` — ajout `@storybook/addon-themes` aux addons |
| 2026-06-29 13:40 | Modifié | `.storybook/preview.js` — import `dark.css` après `all.css` + decorator `withThemeByDataAttribute` (`defaultTheme:'dark'`, `attributeName:'data-theme'`) + `parameters.chromatic.modes: allModes` global → 2 snapshots Chromatic par story (baselines light/dark séparées). Résultat vérifié : `text.primary` light=#202020 / dark=#edeef0, `action.primary` light=#007a68 / dark=#34d3bb. |
| 2026-06-29 13:44 | Créé | `decisions/ADR-065-dark-mode-tokens-storybook-chromatic.md` |
| 2026-06-29 13:44 | Modifié | `decisions/README.md` |
| 2026-06-29 13:44 | Modifié | `tokens/semantic.dark.json` — correction référence ADR-072 → ADR-065 |
| 2026-06-29 — | fix(site) | **6 corrections UI — hero cover, padding, light mode, footer, nav doc, cards.** (1) **Hero image cover** : `figure.v2-art-hero` sort du flux via `position:absolute;right:0;top:0;width:50%;height:100%` ; `img` → `object-fit:cover` ; `.v2-hero .v2-shell` → `position:static` pour que `.v2-hero{position:relative}` soit le containing block ; mobile reset dans `@media(max-width:980px)` (position:static, object-fit:contain). (2) **Hero padding-top** : régression corrigée, `3rem` → `1rem` (64+48px → 64+16px). (3) **Light mode CTA :visited** : `.v2-nav a:not(.v2-nav-action):visited` exclut le bouton "Démarrer" des règles `:visited` ; `[data-theme="light"] .v2-nav .v2-nav-action:visited` force la couleur `on-action` (blanc sur teal) — règle ADR-047. (4) **Light mode doc/editorial cards** : `[data-theme="light"] .v2-doc-block, .v2-editorial-block` → `background-surface + shadow-card` (contraste #f0f0f0/#fcfcfc = ΔL minime, corrigé). (5) **Footer bottom row** : copyright + "Construit avec" extraits de la colonne Explorer → `.v2-footer-bottom` (flex, justify-content:space-between, border-top, margin-top:2rem). (6) **Nav Documentation** : `<button>` → `<a href="documentation.html">` ; JS click handler simplifié : `closeDocs()` + navigation naturelle via href ; survol ouvre le megamenu (inchangé). Images sans variante light (`IMG-HUMANS-AI`, `IMG-KNOWLEDGE-ASSETS`, `IMG-SINGLE-SOURCE`, `IMG-CONTRACTS`, `IMG-AGENTICA`) : aucun fichier `-on-light.png` en Brand/illustrations/ — dette artwork notée. Build : 804 · 270 · 0 fantôme · 98 fichiers · index.html 29 KB. |
| 2026-06-30 15:15 | Modifié | `.claude/rules/code-style.md` |
| 2026-07-02 10:54 | Modifié | `.claude/rules/contexts-utilisation.md` |
| 2026-07-02 21:27 | Créé | `decisions/ADR-066-playwright-testing-strategy.md` |
| 2026-07-03 14:50 | Créé | `.claude/rules/layout-pattern.md` |
| 2026-07-06 09:29 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-06 09:29 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-06 09:30 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-06 09:30 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-06 09:30 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-06 09:30 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-06 09:36 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-06 10:10 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-06 10:51 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-06 11:23 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-06 11:24 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-06 13:18 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-06 13:48 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-06 14:13 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-07 10:32 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-07 10:32 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-07 10:36 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-07 10:54 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-07 13:16 | Créé | `.claude/rules/figma-library-governance.md` |
| 2026-07-07 13:16 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-07 13:16 | Modifié | `.claude/rules/figma-components.md` |
| 2026-07-07 — | fix(tokens)+fix(component) | **Dette de tokens `agtc-feature-card.js` corrigée (audit Figma §22).** 3 défauts trouvés : (1) `background:rgba(12,15,25,.78)` codé en dur → nouveau token `semantic.color.background.overlay-dark` ; (2) `border:1px solid rgba(255,255,255,.06)` codé en dur → nouveau token `semantic.color.border.overlay-dark` ; (3) `var(--agtc-semantic-color-accent, #e35d6a)` référençait un token inexistant (fallback CSS masquait silencieusement le bug) → corrigé vers `--agtc-semantic-color-brand-accent` (déjà existant, `#ed6b86`, primitive `accent.9`). Les deux nouveaux tokens sont des valeurs littérales `rgba()` non aliasées à une primitive — même convention que `overlay.scrim/dark/medium/subtle` déjà en place (aucune primitive n'existe à ces teintes/opacités précises, notamment 6% pour la bordure — l'échelle `white` s'arrête à 18% sur `white.12`). `npm run tokens` régénéré (`dist/tokens/css/all.css` : `--agtc-semantic-color-background-overlay-dark` et `--agtc-semantic-color-border-overlay-dark` confirmés avec les valeurs rgba exactes). Rendu visuel inchangé (fallback CSS = valeur d'origine). `guidelines/components/feature-card.md` : table "Contrat token" ajoutée. Pas de nouvel ADR — application de `tokens-system.md` (dette existante), pas de nouvelle décision architecturale (cf. précédent commit ee457bf, même traitement pour label-bold/detail-bold). |
| 2026-07-07 — | style(component) | **`agtc-feature-card.js` : `.icon-wrap` perd son fond.** Demande explicite utilisateur — `background:rgba(18,165,148,.12)` retiré (dernière valeur codée en dur du composant), `border-radius:8px` retiré aussi (devenu inerte sans fond ni bordure). L'icône reste visible directement sur le fond glassmorphism de la carte, couleur inchangée (`--agtc-semantic-color-action-primary`). Changement purement visuel, aucun nouvel état/variante — pas de revue UX patterns déclenchée. |
| 2026-07-07 13:59 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-07 14:13 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-07 14:13 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-07 14:13 | Modifié | `.claude/rules/figma-components.md` |
| 2026-07-08 10:43 | Modifié | `.claude/rules/figma-library-governance.md` |
| 2026-07-08 10:44 | Modifié | `.claude/instructions/figma-components.md` |
| 2026-07-08 10:44 | Modifié | `.claude/rules/figma-components.md` |
| 2026-07-08 10:44 | Modifié | `.claude/rules/figma-components.md` |
| 2026-07-08 11:58 | Créé | `decisions/ADR-067-letter-spacing-token-category.md` |
| 2026-07-08 11:58 | Modifié | `decisions/README.md` |
| 2026-07-08 — | fix(component)+token(semantic) | **Dette de tokens `agtc-code-block.js` corrigée (seul consommateur de `semantic.typography.mono.family` du dépôt).** 3 valeurs de typographie codées en dur trouvées : (1) `line-height:1.6` (corps du code) → câblé sur `semantic.typography.detail.line-height` (déjà égal à `primitive.lineHeight.reading`, fix mécanique, aucun nouveau token) ; (2) `font-weight:600` (badge de langue) — aucune primitive à 600 (regular/medium/bold = 400/500/700) ; présenté à l'humain, qui a choisi de réutiliser `medium` (500) plutôt qu'ajouter `semibold:600` → câblé sur `semantic.typography.label.weight` (changement visuel mineur accepté) ; (3) `letter-spacing:0.06em` (badge de langue) — aucune catégorie `letterSpacing` n'existait (ni primitive ni sémantique) ; nouvelle catégorie créée et approuvée (Design System Lead + Principal Designer) : `primitive.typography.letterSpacing.{normal,wide}` + `semantic.typography.letter-spacing.{normal,wide}` → **ADR-067**. `guidelines/components/code-block.md` : table "Tokens utilisés" complétée (3 lignes). `npm run tokens` + `node site/build.js` régénérés : 814 définies · 270 référencées · 0 fantôme. Rien touché côté Figma (chantier séparé, ADR-067 le mentionne explicitement). |
| 2026-07-08 — | token(semantic)+fix(site) | **Dette résiduelle ADR-067 réglée : `semantic.marketing.typography.eyebrow` n'avait pas de token letter-spacing.** L'`$intent` du token `eyebrow.size` mentionnait « letter-spacing large » sans câblage réel — la valeur vivait en dur dans `site/build.js` (`--agtc-tracking-eyebrow:.12em`, consommée par `.hero-eyebrow`, `.eyebrow`, `.rd-eyebrow` et les variantes marketing, ~7 sites d'appel). Ajout `primitive.typography.letterSpacing.widest` (0.12em — nommé `widest` et non `wider` pour éviter la collision avec `--agtc-tracking-wider` préexistant du site, qui vaut 0.06em, une valeur différente) + `semantic.marketing.typography.eyebrow.letter-spacing` (alias). `site/build.js` : `--agtc-tracking-eyebrow` repointé vers `var(--agtc-semantic-marketing-typography-eyebrow-letter-spacing)` — aucun site d'appel modifié, valeur résolue identique (0.12em), 0 changement visuel. `guidelines/foundations/contextes.md` : snippet Mode Marketing complété. Application d'ADR-067 (extension de la même catégorie, pas de nouvelle décision architecturale) — pas de nouvel ADR. Note : les 8 autres valeurs `--agtc-tracking-*` du site (tighter/tight/snug/heading/wide/wider/label/loose/overline) restent hors périmètre — dette distincte, plus large, à auditer séparément si souhaité. Build : 815 définies · 271 référencées · 0 fantôme · 101 fichiers. |
