# personnalisation/

This is the only directory a team modifies when adopting the Clone. `core/` (see `../core/README.md`) is never touched.

This file is a placeholder. The full guide — what each subfolder is for, with examples, plus a getting-started checklist so a team doesn't forget a decision it already made (palette, type scale, spacing, etc.) — is a separate, follow-up piece of work.
