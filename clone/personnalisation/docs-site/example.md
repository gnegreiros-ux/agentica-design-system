# Docs-site — example

Configuration the documentation generator reads from this folder at generation time.

This configuration is never versioned in the generated output itself — only here, in the personalization layer.

## Example (fictional — replace with your own)

```json
{
  "siteTitle": "Our Design System",
  "navigation": ["Foundations", "Components", "Patterns"]
}
```
